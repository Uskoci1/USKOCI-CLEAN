name: RU-2 DB apply proof

on:
  push:
    branches:
      - proof/ru2-need-v2-r07-20260903
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ru2-db-apply-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ru2-db-apply:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      SUPABASE_TELEMETRY_DISABLED: "1"
      DO_NOT_TRACK: "1"
      DB_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres

    steps:
      - uses: actions/checkout@v4

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Prepare full canonical predecessor with live bootstrap and aliases
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/uskoci-ru2-proof
          mkdir -p /tmp/uskoci-ru2-proof
          cd /tmp/uskoci-ru2-proof
          supabase init
          rm -rf supabase/migrations
          mkdir -p supabase/migrations

          # Proof-only bootstrap reconstructed from a fresh live read.
          cp "$GITHUB_WORKSPACE/supabase/proofs/ru2_predecessor_bootstrap.sql" \
             supabase/migrations/20260825000000_ru2_proof_live_bootstrap.sql

          for f in "$GITHUB_WORKSPACE"/supabase/migrations/*.sql; do
            b="$(basename "$f")"
            case "$b" in
              20260903130355_clean_ru0_authority_closure.sql|20260903165700_clean_ru1_worker_readiness.sql|20260903190000_clean_ru2_need_v2_draft.sql|20260901105922_clean_completion_and_writer_authenticated_proof.sql|20260901113333_clean_ai_interview_turn_authenticated_proof.sql)
                ;;
              *) cp "$f" "supabase/migrations/$b" ;;
            esac
          done

          # RU-0/RU-1 physical source names are mapped to their actual live IDs.
          cp "$GITHUB_WORKSPACE/supabase/migrations/20260903130355_clean_ru0_authority_closure.sql" \
             supabase/migrations/20260903160812_clean_ru0_authority_closure.sql
          cp "$GITHUB_WORKSPACE/supabase/migrations/20260903165700_clean_ru1_worker_readiness.sql" \
             supabase/migrations/20260903184545_clean_ru1_worker_readiness.sql

          # These two historical migrations are runtime proofs. Fresh inspection
          # confirms their test mutations roll back; only COMMENT metadata survives.
          # Replay exactly those durable effects while keeping the historical IDs.
          cp "$GITHUB_WORKSPACE/supabase/proofs/ru2_replay_20260901105922_durable_effects.sql" \
             supabase/migrations/20260901105922_clean_completion_and_writer_authenticated_proof.sql
          cp "$GITHUB_WORKSPACE/supabase/proofs/ru2_replay_20260901113333_durable_effects.sql" \
             supabase/migrations/20260901113333_clean_ai_interview_turn_authenticated_proof.sql

          supabase db start
          supabase db reset --local

      - name: Verify exact product predecessor
        shell: bash
        run: |
          set -euo pipefail
          product_count="$(psql "$DB_URL" -Atc "select count(*) from supabase_migrations.schema_migrations where version <> '20260825000000';")"
          product_head="$(psql "$DB_URL" -Atc "select max(version) from supabase_migrations.schema_migrations where version <> '20260825000000';")"
          echo "product_predecessor=$product_count/$product_head"
          test "$product_count/$product_head" = "58/20260903184545"
          test "$(psql "$DB_URL" -Atc 'select count(*) from public.ai_structured_facts;')" = "82"
          test "$(psql "$DB_URL" -Atc 'select count(*) from public.ai_conversations;')" = "15"
          test "$(psql "$DB_URL" -Atc 'select count(*) from public.needs;')" = "6"

      - name: Apply RU-2 candidate
        shell: bash
        run: |
          set -euo pipefail
          psql "$DB_URL" -v ON_ERROR_STOP=1 -f "$GITHUB_WORKSPACE/supabase/migrations/20260903190000_clean_ru2_need_v2_draft.sql"
          psql "$DB_URL" -v ON_ERROR_STOP=1 -c "insert into supabase_migrations.schema_migrations(version,name) values ('20260903190000','clean_ru2_need_v2_draft');"
          product_count="$(psql "$DB_URL" -Atc "select count(*) from supabase_migrations.schema_migrations where version <> '20260825000000';")"
          product_head="$(psql "$DB_URL" -Atc "select max(version) from supabase_migrations.schema_migrations where version <> '20260825000000';")"
          echo "post_ru2=$product_count/$product_head"
          test "$product_count/$product_head" = "59/20260903190000"

      - name: Verify static RU-2 invariants
        shell: bash
        run: |
          set -euo pipefail
          test "$(psql "$DB_URL" -Atc "select count(*) from public.ai_structured_facts where fact_schema_version='LEGACY_TEXT_V1';")" = "82"
          test "$(psql "$DB_URL" -Atc "select count(*) from public.ai_conversations where fact_schema_version='LEGACY_TEXT_V1';")" = "15"
          test "$(psql "$DB_URL" -Atc 'select count(*) from private.need_fact_registry;')" = "20"
          test "$(psql "$DB_URL" -Atc "select has_function_privilege('authenticated','public.rpc_ai_apply_interview_turn_v2_service(uuid,uuid,text,text,text,jsonb)','EXECUTE');")" = "f"
          test "$(psql "$DB_URL" -Atc "select has_function_privilege('service_role','public.rpc_ai_apply_interview_turn_v2_service(uuid,uuid,text,text,text,jsonb)','EXECUTE');")" = "t"
          test "$(psql "$DB_URL" -Atc "select has_table_privilege('authenticated','public.need_sensitive','UPDATE');")" = "f"
          test "$(psql "$DB_URL" -Atc "select count(*) from public.needs where status='DRAFT';")" = "0"

      - name: Stop disposable stack
        if: always()
        shell: bash
        working-directory: /tmp/uskoci-ru2-proof
        run: |
          supabase stop --no-backup || true
