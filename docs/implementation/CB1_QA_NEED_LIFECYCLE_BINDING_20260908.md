# CB1 capability binding: pre-Dogovor Q&A and Zadatak terminal commands — 2026-09-08

Status: CLIENT SOURCE IMPLEMENTED / TYPECHECK AND JEST PASS / NO SERVER CHANGE / NO SCREEN BOUND / Q&A FAIL-CLOSED ON LIVE BY SERVER DESIGN.

An inventory of the live `public.rpc_*` surface against every `rpc` reference in `src/` showed seven authenticated capabilities the client never calls: the five RU-4B clarification RPCs (`rpc_ru4b_ask_preselection_question`, `rpc_ru4b_answer_preselection_question`, `rpc_ru4b_disposition_preselection_question`, `rpc_ru4b_owner_preselection_questions`, `rpc_ru4b_public_preselection_qa`) and the two Requester terminal commands (`rpc_cancel_need`, `rpc_delete_draft_need`). The inventory also showed that the `rpc_r24_*` / `rpc_r25_*` / `rpc_ai_complete_conversation` names still referenced in `src/data/ports.ts` exist only in comments: no client code path calls a retired donor function. This unit binds the seven capabilities as standalone client owners and corrects the comments to the live names.

Q&A (`src/contracts/preselectionQa.ts`, `src/data/preselectionQaClientService.ts`). Every command carries one uuid request id (the server column is `uuid`; `noviUuidZahtevId()` in `src/lib/idempotencija.ts` produces it, cryptographic RNG when available, `Math.random` v4 otherwise — the key needs uniqueness, not secrecy) and refuses a non-uuid id before touching the server. Receipts report `idempotentReplay` as replay. Projections never carry an asker identity because the server never returns one. The server's exception names are mapped once to Vi-form copy: the public floor (`PHONE_NOT_PUBLIC`, `EMAIL_NOT_PUBLIC`, `OFF_PLATFORM_LINK_NOT_PUBLIC`, `SOCIAL_HANDLE_NOT_PUBLIC`), stale revision, ownership, materiality (`RU4B_MATERIAL_REQUIRES_RU4_EDIT` → "Izmenite Zadatak umesto odgovora"), and the fail-closed authorities. On live today `private.ru4b_assert_block_authority_ready` and `private.ru4b_assert_rate_authority_ready` unconditionally raise, and `private.ru4b_has_exact_policy_allow` finds no reviewed bundle (zero rows in `publication_policy_bundles`), so every ask and answer resolves to "Pitanja o Zadatku još nisu dostupna." until P5 REPORT/BLOCK, a rate policy and the D-0140 bundle exist. The read projections work today (owner list, public list) and return empty.

Zadatak terminal commands (`src/contracts/needLifecycle.ts`, `src/data/needLifecycleClientService.ts`). `cancelNeed(needId, expectedRevision, reason)` and `deleteDraftNeed(needId, expectedRevision, reason)` are revision-bound so a stale screen never acts on a Zadatak the user did not see, replay idempotently, and surface the server's boundaries in product language: a Zadatak with a Dogovor is routed to the Dogovor lifecycle (`NEED_CANCELLATION_REQUIRES_AGREEMENT_FLOW`), only an unpublished draft without media or history can be deleted, `STALE_REVIEW_REQUIRED` asks for a refresh, `FORBIDDEN` says whose Zadatak it is.

Not bound to screens. R04 (`pregled.tsx`) is the natural home of "Otkaži Zadatak" and R07 of draft deletion, but R04 is also modified by the open T1b unit (#73); binding into screens follows once that merges and stays within existing components (no final shared-component look). The Q&A screens (W02 public Q&A, R04 owner questions) are gated on the design track.

Proof: `tsc` clean; jest `cb1-preselection-qa-client.test.ts` (9) and `cb1-need-lifecycle-client.test.ts` (5) plus the full suite recorded in the PR. No disposable database proof: no server object changes.

Limits retained: no server change; no screen binding; Q&A fail-closed on live by server design; no device proof.
