# D-0140-A publication policy bundle registration — 2026-09-08

Status: SOURCE IMPLEMENTED / LOCAL ADMISSION PASS / DISPOSABLE PROOF PENDING ON RUNNER / NOT LIVE / BUNDLE REGISTERED UNREVIEWED, INCOMPLETE, INACTIVE.

Need publication and RU-4B clarification Q&A are fail-closed on live because `private.publication_policy_bundles` has no row: `private.current_publication_policy_bundle` returns NULL, `rpc_record_need_publication_decision_service` raises `POLICY_BUNDLE_NOT_READY`, and `private.ru4b_has_exact_policy_allow` finds nothing. The RU-3 B05 foundation deliberately seeded no content and states that activation requires reviewed and complete metadata. The only owner-approved policy content that physically exists is `docs/implementation/ru3/RS_PUBLICATION_POLICY_MINIMUM_OWNER_LOCK_V1.md` (status `OWNER_LOCKED_MINIMUM / NOT_PRODUCTION_ACTIVATED`, blob sha256 `792597eb4b5b940238f784587c9431613d3a71bdd7f662e9439fdbed134bd2aa`): sixteen rule ids `RS-MIN-001`…`RS-MIN-016` with outcomes, jurisdiction RS, version 1.

This unit registers that baseline as one bundle row (`policy_id` `RS_PUBLICATION_POLICY_MINIMUM`, version 1, jurisdiction `RS`) with `is_reviewed=false`, `is_complete=false`, `is_active=false`, provenance pointing at the source document and its digest, plus sixteen `publication_policy_rule_refs` whose provenance carries only the outcome copied verbatim from the document and an explicit `NOT_SEEDED` marker for the text. Nothing else changes: no function, no grant, no review stamp, no activation. The migration's own postcondition proves the resolver still returns NULL and readiness is false. The `policy_id` string is a technical identifier chosen here (no consumer existed to define one); it is not legal content.

What this moves. Before: three gates stood between a draft and public inventory — bundle content, bundle review/activation, and the service ALLOW gate (`RU3_ALLOW_NOT_ENABLED`). After: the content is registered with stable rule ids the evaluator can cite, and two authorized-operations acts remain — (1) legal review and completeness confirmation on this row (`is_reviewed`, `reviewed_at`, `is_complete`, then `is_active`, `activated_at`), (2) lifting the ALLOW gate in the RU-3 service RPC — both OWNER DECISION REQUIRED, neither performed by this unit. A policy evaluator (the service that reads a draft, applies the rules and records the decision) is a separate engineering unit that this registration makes addressable.

Proof. `supabase/proofs/policy/d0140a_bundle_registration_proof.mjs` on the runner: historical79 → N07/N08 → exact frozen D03 and AI DRAFT to live87 → this file as 88. Six checks: predecessor without any bundle; exact apply with unchanged history, exactly one bundle with the expected flags and provenance and the exact sixteen rule id → outcome map without rule text; the registered bundle is inert (resolver NULL, not ready, no Q&A allow, and the service gate refuses ALLOW with `RU3_ALLOW_NOT_ENABLED`); the activation gate is schema-enforced (active without review, review without stamp, activation without `activated_at` all refused), a disposable-only rehearsal of the authorized activation makes the resolver return the bundle while still admitting nothing and still refusing ALLOW, duplicate version and second active bundle refused, then reverted; rule refs restrict deletion, refuse duplicates and blanks, and neither authenticated nor service_role can read either table; final fingerprints, history, zero active bundles, zero decisions, zero rule text. The admission test additionally re-derives the sixteen rules from the source document and fails if the document or the seed drifts.

Limits retained: no evaluator; no review; no activation; no ALLOW; no UI; no device proof.

## 2026-09-09 canonical reconciliation checkpoint

The branch was merged forward, without force push, through canonical `cc71a93b5ca616ea476c96ffe207833b2210cd57` (PR #78 and PR #80 already integrated). The only merge conflicts were the shared migration inventory files. Their resolution preserves every canonical migration entry and adds only `20260908170000_clean_d0140a_policy_bundle_registration.sql` plus its D0140-A provenance. The migration bytes remain MD5 `48b05badae57b036537072b18cdac313`; `live_applied` remains `false`; the registered state remains `UNREVIEWED_INCOMPLETE_INACTIVE`.

This reconciliation does not authorize or perform Supabase promotion, legal review, activation, `RU3_ALLOW_NOT_ENABLED` lifting, Q&A publication admission or any visual change. The exact reconciled branch must pass its disposable D0140-A proof and repository integrity checks before canonical merge.
