# Intent shell / first UI reconstruction slice — 2026-09-07

The former five-tab shell opened a redundant Home, displayed Profile as a permanent tab, and exposed a placeholder discovery mode. The Profile screen showed invented identity/reputation and inactive actions. This slice installs the current owner's three-zone navigation and real own-profile reads while retaining the admitted marketplace services.

The owner's latest steering keeps this implementation a **testable functional scaffold**. Future Figma work owns the final visual design; Fable is separate asset work. Current capability and navigation remain testable without treating this slice as a final visual redesign. Frozen governance bytes remain unchanged.

Authority: the current session explicitly adopts Execution Master V2 and both 06.09 UI documents, now preserved under `docs/governance/2026-09-07/`. The later V3 execution addendum was read in full and adopted during this slice without repeating admission or discarding in-flight work; V3 supersedes older execution method where different. The frozen 03.09 governing package retains its backend/owner authority (SHA-256 `e063b050dd673485ebb9b1d3e3a556fb0c88dbdda4bacc95eacbf760a31ae988`); newer documents supersede its conflicting UI/navigation assumptions. Admission fetched canonical `7a94c19f8f79a97a91cec45f21c6e349345b122e`, inspected PR45/46/47 and successful current gates, and physically confirmed live project `leqcwgzvjsxugfgzdmth`. N07 promotion has separate provenance and does not depend on this UI change.

## Implemented boundary

- MENI TREBA: Zadaci / canonical U with Novi / Dogovori. JA MOGU: Prijave / canonical U with Zadaci / Dogovori. Compatibility `/` redirects to the appropriate existing root. All route URLs remain registered; hidden leaves cannot become extra tabs. Existing bottom safe-area clearance remains.
- Root lists expose the real Inbox bell and an avatar leading to the appropriate own-profile hub. Explicit mode switching opens requester tasks or worker discovery. Repeated navigation uses existing destinations; mode/profile actions acquire a synchronous guard before navigating.
- Profile reads only the authenticated account's matching REQUESTER or WORKER identity fields. Foreign/malformed scope is rejected; missing, loading and failure remain distinct. Real worker editing and local-device sign-out are available. Invented ratings and inactive profile/review/settings rows are removed.
- R03, W03, Dogovori and profile reads belong to a focused account, identity revision, intent and request generation. Focus and foreground reread; late completion after blur/account change/intent change or a newer retry cannot publish. Errors clear current eligibility/identity and expose retry. Root is keyed by account id and monotonic identity revision, so batched A→B→A remounts private state while same-account token refresh retains it.
- UI-issued Auth commands cross the SDK-free Auth contract into the central Auth client service. Existing payloads, errors, local logout scope and Auth-runtime session authority remain intact. Profile logout validates its captured account/revision before beginning the SDK command. See `AUTH_CLIENT_BOUNDARY_20260907.md` for the reviewed boundary and concurrency regressions.
- W03 is a real discovery list. An actual source error no longer produces a false empty result. Kombinovano and the placeholder map control are absent. Whole cards open W04; empty results lead to the existing worker editor. Root empty states offer a next action.
- Proof assets use LF checkout bytes so Windows does not corrupt the existing exact candidate-versus-migration assertions. No SQL contents or applied migration bytes change.
- Every private root route is excluded by the installed Expo Router `Stack.Protected` while signed out; Auth is excluded while signed in. This prevents the hidden root redirect from winning over the Auth effect. Account-keyed navigation remains in place.
- Session startup reads cannot overwrite a later Auth event. Deferred intent work and both route effects check the current session epoch and account. Return-target completion compares the exact pending revision and intent inside the existing storage lock; completion signals a new read by the root. Logout/account-switch cleanup can retry after storage failure without adopting the old account's target or deleting a newly prepared target. Stale completion/consumption restores the record before releasing the same lock.

## Verification and limits

Initial shell checks passed 38 suites / 232 tests. After the native-discovered Auth repair and independent session review, combined TypeScript, migration integrity and 42 Jest suites / 265 tests passed. The later canonical URL decoder dependency brings two separately verified real Router tests; the final PR pipeline checks their combination. Existing navigation/safe-area assertions now test the newer three-zone authority; 24 added tests cover own-profile scoping and interaction, request ordering, focus invalidation and discovery failure/retry. These are source/component checks, not native navigation proof. Android run, exact source, artifacts and inspection results are recorded below when available.

After the latest Auth boundary and identity-revision correction, **46 Jest suites / 313 tests and TypeScript PASS**. Root reviewed the complete correction with no blocker. A reproducible TypeScript AST scan of 62 client source files / 25 presentation files has zero findings for its stated SDK/network/secret-reference rules; this is not a complete AI/domain-authority audit. These source checks do not extend an earlier native artifact to changed code.

This is a bounded shell and root-read slice, not closure of all 28 surfaces. It does not establish final task-centered multi-person Dogovor, hard calendar conflict authority, W02 AI profile, map/filter/viewport persistence, reviews, requester profile editing, account export/closure, native push delivery, media, publication-policy activation, iOS or Store readiness. W06's existing application mutation protocol and other detail/Chat lifecycle gaps remain separate work. Current platform charge remains 0 RSD; HITNO, D0140, public Q&A and unsupported controls remain gated.

## Physical proof

First run `34104812406`, source `6203c5eb74bf4eb2c8d7b03178c41c231045cf5a`, built the APK and passed local endpoint/KVM gates, then failed before Auth input: the original PNG/XML showed a signed-out private shell. Artifact `10012595114` ZIP SHA-256 `279082b0aa488e6a05481b6d12bc0e24d66f4ce51feeb8b3755c209d6435656c` was downloaded and matched API metadata. The failure is not accepted proof. It caused the navigator protection repair above; a new whole journey is required.

The updated harness requires real Auth controls and absence of private tabs before login and after UI logout. It preserves the N04 journey, adds both intent zones/profile/Back paths and a second account in the same app storage. Tapping Novi legitimately opens one owned local AI conversation, explicitly counted in the evidence; the later navigation slice asserts no Application/Selection mutation. All fixtures and those expected writes belong to the disposable project, never production.

Prior accepted N04 and RU5 runs remain historical proof at their exact source boundaries; they do not prove this changed shell. Final native evidence follows here after inspection.

Run `34110549395` at `a8075b6f76c82d3e0a565db4be7c9526765fffb1` completed successfully. Job `101705515424`, artifact `10015039759`, ZIP SHA-256 `d980253d8877009f17662724ca89baf80ed68a68d10c6fd33a216207d54e1e22`, APK SHA-256 `1705a3a491b70e6a19893d8b622893c959e499f566e7dd1e3335c23ace766586`. All 27 original PNG/XML pairs were inspected: real Auth, both intent zones, avatar Profile, Back, Inbox and UI logout/account B in the same app storage passed. Postflight records the expected one local NEED_INTAKE / NEED_FACT_V2 conversation opened by Novi and no extra response/selection mutation. This is Android emulator proof against a disposable Supabase instance; it is not physical hardware, production push or Store proof.

Original visual inspection of that pre-fix run caught a real defect: `NAV_worker_agreements.png` let the long counterpart name push `Fleksibilno`, `3.000 RSD` and the chevron offscreen; the requester row crowded schedule and amount. Participant identity now has its own bounded wrapping row. Schedule and the full amount occupy a separate wrapping row, keeping currency intact. The replay additionally requires schedule and amount to be within their own Agreement card and screen for both roles. The corrected source replay below resolves that concrete defect.

Two intervening failed attempts remain historical: run `34108000350` stopped in Jest before an APK because of the canonical official decoder alias's ESM allowlist; the installed Expo preset allowlist is now preserved and that alias added. Run `34108536812` built the APK but a Quickstep system launcher ANR overlaid the Auth capture between two UI dumps. The harness now asserts against the same atomically sampled UI tree used by its wait; USKOČI app ANRs still fail. These failed runs do not count as accepted device proof.

The final source also includes canonical PR #53/#54 N08 source and live85 provenance. Its integration does not reapply or modify any migration bytes.

Run [34114525948](https://github.com/Uskoci1/USKOCI-CLEAN/actions/runs/34114525948) at exact source `86fffc1b58789a2a7331e3646ba8aa0647e02a73` passed, including all six source checks and three CodeQL analyses with zero errors/results. Artifact `10016574410` original ZIP is 4,145,359 bytes, independently matched to API SHA-256 `c2d067810d728571780ddca423cc09ac1bf7aa9f9a494b1a774e20c4a2e33767`; runner APK SHA-256 is `d3c6f3ae7188962159805a458489c88698e61a8d25e08d0f79f964445c2fb46b`. All **27 original PNG/XML pairs** were inspected individually. Both Agreement rows retain readable counterpart names, actual `Fleksibilno`, full `3.000 RSD` and chevrons; original XML confirms schedule and amount inside the card and screen for both roles. Full scoped inspection and pair fingerprints are in `evidence/intent-shell-auth-20260907/run34114525948/`.

Source86 is accepted only for that recorded Android emulator/navigation/Auth-account/Inbox boundary. The subsequent Auth service and `accountRevision` correction requires **new exact-head CI/CodeQL and Android replay**. PR49 remains unmerged pending those gates and the required architecture review. The next functional AI vertical has a separate authority review; neither final visual design nor complete AI/product closure is claimed here.
