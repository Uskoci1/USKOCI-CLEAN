# Home attention continuation — 2026-09-22

Latest supplement: PKG-043/044 restored owner-observed intake transport after Gemini400 and fixed the
guard that overwrote contextual ASK prose with generic field questions. Owner confirmed improvement
and requested warmer replies. Prompt-only refinement d73d8c5a is deployed as intake50, byte-verified,
JWTtrue, anonymous401; CI35704269701 passes types,355 offline Edge tests and25 disposable integration
checks. No SQL/client change; installed APK35698097121 remains current. Owner's PKG-043 retry produced
two SUCCEEDED receipts and phone replies. New tone quality remains for owner feedback on his next reply.
Read package043/044 docs and current status before relying on historical AI state below. No agent-paid
provider call or voice interaction occurred. Runtime generation is still only owner-initiated.

Read the latest owner messages, mandatory 20260921_2145 handoff and AGENTS.md first. This supplements
`NEXT_AI_HANDOFF_20260922_AI_REPAIRS.md`; it does not grant permissions or reopen completed packages.
Work solo in the canonical checkout, branch work/pre-v3-engine-integration-20260911. Commit/push allowed;
no PR/force-push/repair branch. Do not rewrite source147 or include the owner's foreign untracked SQL.

## Verified state

- DEV leqcwgzvjsxugfgzdmth ledger196 =147 source +49 dev_alpha, after PKG-042a.
- Closure65980fce17030f1d8b34177b8989549c2144bf806478238af39dec04b137a591 unchanged. Candidate asserted
  live=certified and readiness before, unchanged live/readiness before commit. Certified table readback
  matches. Direct private digest calls were not executed; historical connector restriction still applies.
- Migration20260922071319 dev_alpha_pkg042a_cancelled_agreement_read_parity equals the committed SQL
  without its final LF: f0b7356c9f66067e91b2acf63a7f30a6729debc22e1f0e2bc45af6524e145670.
  All three resulting body pins and the two unchanged classifier/reference pins match. Owners, ACLs,
  SECURITY DEFINER flags, search_path, strictness and volatility match preflight.
- Proof35698097056 on dfa54206a0bc34027d1e545bb2b31ba60909c804 passes25 SQL/Auth/REST checks including
  loading the exact client adapter, plus346 offline Edge tests. Downloaded hashes checked against git.
  PKG-00435698097011 and PKG-00735698097030 pass on the same source.
- Types pass; focused Home60 tests pass. Three new native route regressions fail before/pass after.
  Full repeat242 suites /4687 tests passes, exit0. First full run had one unchanged Firebase subprocess
  deadline failure (4686 pass); isolated8/8 then full repeat pass. Existing delayed-exit warning recorded.
- No Edge deployment in042. Prior intake47 / worker17 / Q&A13 / publication14 and JWTtrue are unchanged
  by this packet; see AI handoff for their deployment evidence. No paid provider call or phone action.
- APK35698097121 source dfa54206 succeeded; downloaded68,554,979 bytes, SHA256
  3dfddf496163f860f5b9e526d8bd3d291f031526534a9b6f7070b3cd9efa0fde. Checksum/source/tree and both
  recovery/icon attestations match. File artifacts/apk-35698097121/USKOCI-DEV.apk. Subsequently installed
  on the owner's USB phone at their explicit request, with adb install -r. Installed hash matches;
  cold launch and signed-in Home rendering observed. No agent AI/provider call, speech or whole-journey
  test. See pkg042/DEVICE_INSTALLATION_RECEIPT_20260922.json for the separate launch smoke evidence.

## Change and remaining boundary

PKG-029b corrected only the unpaged application reader. Three other functions still inferred SELECTED
from any historical Agreement, including CANCELLED. PKG-042 repairs their classifier input, preserving
raw SELECTED precedence, stale precedence, history links and product rules; no response row rewrite.

Native Home calls paznjaZaPocetnu independently of three preview readers. The strict decoder validates
version, timestamps, subject bindings, count arithmetic and reason ordering before producing existing
Serbian wording/navigation. A failed aggregate stays unavailable with retry; no local-inference fallback.
Attention survives other preview failures and late results cannot cross accounts. The legacy pure
composition stays for SQL reference proofs and explicit test source only.

The other three reads still load full activity/upcoming lists. Do not page them blindly, claim payload
reduction or mark complete Home scalability solved. Freshly reading `moje-prijave.tsx` confirmed its
pending command already calls `readApplicationCommandState(pending.row)` and never settles from the
displayed list. Existing native tests cover an omitted named row and failed/late read; they pass in the
full run. Preserve that completed prerequisite when introducing pagination; do not repeat the obsolete
September19 claim that named-row reconciliation is missing. New layout/design was not part of042.

## Continue in this order

1. Confirm current source/DEV first; never reapply042. Check APK receipt/current status for latest verified
   build and owner phone readiness. No installation, speech, data clearing or uninstall by implication.
2. Public task column privacy7.17: define public detail + owner/participant read contracts and rollout
   compatibility. Read PUBLIC_TASK_PRIVACY_INVESTIGATION_20260921.md before any table grant change.
   Current installed clients use direct needs reads, embedded/computed fields and owner filter columns.
3. Closure legacy readiness7.41 / second-device8.18: read the CODEX handoff's actual guard and request-key
   findings. A new getter alone is blocked for restricted accounts. Certified changes need separate
   explicit owner permission, even though ordinary proven DEV fixes already have standing approval.
4. Finish remaining RPC family audit7.1 and bounded Home preview design. Preserve refusal/recovery
   semantics, ordering, counts and account isolation. Prove on disposable SQL + actual client/REST.
5. Continue real-device marketplace journey and design plan once the required owner input is present.
   Do not turn on push, invent legal text/operator data, implement an unanswered phone field or select
   daily/recurring pricing from silence. No paid quality probes or new dependencies are authorized.

Proof/DEV receipts and exact scope: docs/implementation/v5-ai-first/pkg042/.
Status index: docs/implementation/USKOCI_CURRENT_STATUS.md. Finish/release/growth gates remain open.
