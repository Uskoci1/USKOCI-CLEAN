# P0E completion truth guards — 2026-09-08

Status: SOURCE IMPLEMENTED / LOCAL ADMISSION PASS / DISPOSABLE PROOF PENDING ON RUNNER / NOT LIVE.

Three live87 owner RPCs let the product state something untrue. `rpc_confirm_completion` guarded only `ALREADY_COMPLETED`, so a `CANCELLED` Agreement could be set to `COMPLETED`. `rpc_mark_work_done` recomputed `statement_timestamp() + 48 hours` on every call, so a retry after an unknown outcome silently moved the Requester deadline. `rpc_report_problem` updated `agreement_execution` idempotently but inserted a new system message on every repeat. All three were confirmed by reading `pg_get_functiondef` bodies whose `md5(prosrc)` values are frozen below and independently matched by a second same-day review.

This forward unit replaces exactly those three bodies from the approved MASTER drafts `06_DRAFT_EVIDENCE/P0_SQL_DRAFTS_V10/P0E` 01, 02 and 04 and the `P0E_PROOF_MATRIX.csv` scenarios, rebuilt against the actual live87 predecessor. Three draft corrections were required: the drafts declared a `need_id` variable while selecting a `need_id` column (plpgsql ambiguity), so the variable is `v_need_id` with qualified column references; the draft completion path updated `public.worker_calendar_commitments`, which does not exist in CLEAN, so that statement is removed and its absence guarded; and `rpc_confirm_completion`/`rpc_report_problem` change return type from `void` to `jsonb`, which `create or replace` cannot do, so they go through `drop function` + `create function` + explicit re-grant. `rpc_mark_work_done` keeps `returns timestamptz`.

Resulting contract. Worker mark: one `CONFIRMED → AWAITING_REQUESTER` transition with a server 48h deadline and one `COMPLETION_REQUIRED` event; replay while awaiting returns the original deadline with no UPDATE and no second event; `CANCELLED`/`COMPLETED` deny with `AGREEMENT_CANCELLED`/`AGREEMENT_ALREADY_COMPLETED`; only the worker may mark. Requester confirm: valid from `CONFIRMED` or `AWAITING_REQUESTER`; `COMPLETED` replay returns `idempotentReplay:true` and changes nothing; `CANCELLED` never resurrects; an open problem blocks the service tick but not explicit Requester authority; one `EXECUTION_STATE_CHANGED` event to the worker; parent Need completion still flows through `private.sync_need_completion`. Problem: parties only, live Agreement only, first narrative and reporter preserved, one system message and one `RECOVERY_OPENED` event on first report, repeat reports acknowledge with `idempotentReplay:true`. Lock order stays Need → Agreement → Execution as in cancel/selection. All three event types were already admitted by the `user_activity_events` CHECK and routed by `private.category_of_event`; no event-type, schema, provider, writer or publish change occurs.

Client compatibility needs no change: `supabaseIzvor.ts` and `agreementClientService.ts` read only `error` from confirm and report-problem, and `rpc_mark_work_done` still returns the deadline that `cdl-a07-completion-mark-equivalence` and `m07-zavrsetak` assert. The RU-0 SECURITY DEFINER manifest rows keep identical identity arguments and `USER_COMMAND` class.

The candidate and forward source are byte-identical: `20260908120000_clean_p0e_completion_guards.sql`, 14,279 UTF-8 LF bytes, MD5 `5354a754bb74a588de33070015aa41e4`, SHA-256 `f558af3866faec14df690780d6b63bd45ab38ab8b3efa6108f27c575194e7a8e`. Predecessor guards: `rpc_mark_work_done(uuid)` `02c0d47fecd9a1a9d565f0b0f3c9c2e1`, `rpc_confirm_completion(uuid)` `39ff23fd1d3f4668dbb99a664bf963a9`, `rpc_report_problem(uuid,text)` `d872dd1eaffdb67856ce6c47f4176f33`, plus `private.emit_event` 12-argument signature, `private.sync_need_completion`, absence of the calendar table and admitted event types. 5s lock/30s statement bounds protect admission. Provenance records the file as `PENDING_FORWARD_MIGRATION` against live87 head `20260907135905`; the frozen 87-file historical inventory SHA-256 is `52561c1019777c5517128e2b06e45ef79f83bdea6d2149e9fd107500e7351cf4`.

Proof entrypoint: `supabase/proofs/completion/p0e_completion_guards_proof.mjs`; predecessor integration: `p0e_completion_guards_predecessor.mjs` with pure admission tests; workflow: `.github/workflows/p0e-completion-guards-proof.yml`; manifests: `p0e_completion_guards_files.json` and `p0e_completion_guards_predecessor_files.json`. The runner reconstructs historical79, canonical N07/N08 to 85, then the exact frozen D03 and AI DRAFT sources to 87 with recorded statements and verified resulting bodies, and applies this file as 88. It first reproduces all three predecessor defects on real disposable accounts through actual `rpc_submit_response`/`rpc_select_response` Agreements, then proves thirteen checks: exact replay deadline, authority/absence boundaries, completion from both live states with idempotent replay and parent Need completion, cancelled non-resurrection through all three commands, single problem message/event with narrative preservation and input boundaries, open problem blocking the tick but not explicit completion, tick auto-completion with later replay, two observed PostgreSQL lock interleavings (service tick holder vs Requester confirm waiter, and the reverse) converging on exactly one `COMPLETED` state, and denied direct table writes. Existing client regressions run afterwards.

Local source checks on this Windows host: migration integrity `files=88 live_snapshot=87 pending=1` PASS, MD5 manifest PASS, Node syntax PASS, predecessor admission tests PASS, TypeScript and the full jest suite recorded in the PR. No Docker/PostgreSQL runtime is installed here; the real disposable proof runs on the GitHub runner. No production, provider or device call occurred and the live database was only read.

Limits retained: the service tick path predates this unit and emits no worker event on auto-completion; that is P0E-03/notification-vertical work, recorded by the proof as an observation, not fixed here. P0E-05 cancellation calendar release depends on a calendar authority CLEAN does not yet have. Global deadlock freedom across all Agreement commands is not claimed; the two interleavings observed are the completion race pairs from the proof matrix. Live promotion follows the existing canonical review → forward-promotion path and is not performed by this branch.
