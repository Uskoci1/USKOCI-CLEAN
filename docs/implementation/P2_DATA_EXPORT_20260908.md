# P2 data-export request ledger — 2026-09-08

Status: SOURCE IMPLEMENTED / LOCAL ADMISSION PASS / DISPOSABLE PROOF PASS ON RUNNER (run 34233662963) / NOT LIVE / ARTIFACT GENERATION NOT ADMITTED.

CLEAN has no server-owned way for an account to request a copy of its data and no honest status of such a request. The donor closed the intake boundary in `rc2_023_data_export_request_foundation` (160 lines); the master's donor→CLEAN reconciliation marks it ADAPT_HIGH. This unit ports the design as one forward-only migration: one public table (RLS forced, every client privilege revoked), three authenticated RPCs, no change to any existing object. It records requests; it does **not** claim that an export artifact exists. Artifact generation, secure delivery, the external DSR channel and the operational SLA remain separate owners and proofs.

Rebinding. `private.r24_require_user()` → inline `auth.uid()` / `AUTH_REQUIRED (28000)`; `clock_timestamp()` → `statement_timestamp()`; parameters `p_client_request_id` / `p_receipt_id`; names `rpc_get_data_export_status()`, `rpc_request_data_export(text)`, `rpc_cancel_data_export(uuid)`. Added over the donor: a per-account `pg_advisory_xact_lock` so both the same-key race (one row plus one replay) and the different-key race (one open request, explicit `DATA_EXPORT_REQUEST_ALREADY_OPEN`) converge without unique violations; `INVALID_RECEIPT_ID` for a null receipt; `DATA_EXPORT_REQUESTED` and `DATA_EXPORT_CANCELLED` audit rows through `private.audit_marketplace` (`SYSTEM`, receipt id, actor = account).

Contract. Status projects the latest request with `downloadAvailable:false`, `serverFulfillmentRequired:true`, `externalDsrChannelReady:false` — always, including READY. Request validates the id (16–96, `[A-Za-z0-9_-]`), records once, replays the receipt for the same id, refuses a second open request until the first reaches a terminal state (READY/FAILED/EXPIRED/CANCELLED). Cancel is owner-only (a foreign or unknown receipt is `DATA_EXPORT_REQUEST_NOT_FOUND`, no existence leak), idempotent for CANCELLED, and refused once the request left REQUESTED. State transitions beyond cancel (PROCESSING, READY, FAILED, EXPIRED) are trusted-server acts with no RPC here. Constraints keep `cancelled_at` ⇔ CANCELLED and `completed_at` ⇔ READY/FAILED/EXPIRED.

Client. `src/contracts/dataExport.ts` and `src/data/dataExportClientService.ts` are the single client owner: `downloadAvailable` is pinned to false regardless of a future projection until a delivery unit exists; replay is reported as replay; server exception names are mapped once to Vi-form copy. No screen binds it yet (S07 rows are gated on the design track).

Proof. `supabase/proofs/legal/p2_data_export_proof.mjs` on the runner: historical79 → N07/N08 → exact frozen D03 and AI DRAFT to live87 → this file as 88. Nine checks: predecessor without export objects; exact apply with unchanged history, table ACL and function grants; honest empty status with anon and service refused on every RPC; request recorded once with replay, key validation, audit and per-account isolation; second open request refused through REQUESTED and PROCESSING, cancel refused while PROCESSING, trusted duplicate-open insert refused by the partial index, READY requires `completed_at`, READY still exposes no download, next request accepted after terminal; cancel owner-only, idempotent and state-bound with audit; direct select/insert/update/delete denied; two observed per-account lock interleavings (different keys → one open request, same key → one replay); final fingerprints, history and no artifact generator. Local: migration integrity `pending=1`, MD5 manifest, Node syntax, admission tests, TypeScript and jest recorded in the PR.

OWNER DECISION REQUIRED (not blocking this unit): the fulfilment path (who generates the artifact, how it is delivered securely, the response deadline) and the external DSR channel — none of these values exist in the available authority documents.

Limits retained: no UI; no artifact generation or delivery; no PROCESSING/READY transition RPC; no device proof.
