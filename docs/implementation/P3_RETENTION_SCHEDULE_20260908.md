# P3 retention schedule registry — 2026-09-08

Status: SOURCE IMPLEMENTED / LOCAL ADMISSION PASS / DISPOSABLE PROOF PENDING ON RUNNER / NOT LIVE / SCHEDULE MISSING_APPROVED_CONTENT.

CLEAN has no server-owned inventory of the personal-data classes it holds and no versioned, counsel-referenced retention schedule (category → purpose → period → deletion trigger → exception → legal basis). The donor closed the technical foundation in `rc2_024_versioned_retention_schedule_foundation` plus its publisher ambiguity repair; the master's donor→CLEAN reconciliation marks it ADAPT_HIGH because the donor's class list names surfaces CLEAN does not have. This unit ports the design as one forward-only migration: three private tables, two RPCs, no change to any existing object, and no purge or deletion worker.

Physically verified CLEAN data classes (the only seeded rows, each citing tables that exist at live87 or the private `profile-media` bucket): `ACCOUNT_IDENTITY`, `PROFILE_DATA`, `NEED_PUBLIC`, `NEED_SENSITIVE`, `RESPONSES_SELECTION`, `PRESELECTION_QA`, `AGREEMENT_CORE`, `AGREEMENT_MESSAGES`, `LEGAL_CONSENT` (its tables arrive with the earlier-ordered forward P1), `NOTIFICATION_DELIVERY`, `AI_VOLATILE`, `MEDIA_OBJECTS`, `COMMAND_LEDGERS` (CLEAN-specific idempotent command ledgers), `AUDIT_SECURITY_LOGS`. Not seeded because CLEAN has no such surface yet: `REVIEWS_REPUTATION`, `SAFETY_SUPPORT`, `COMPLAINT_EVIDENCE`, `ORGANIZATION_DATA` — each enters through trusted operations with its own unit, and the proof shows that admitting a new required class reopens the published schedule.

Rebinding. `private.r24_require_user()` → inline `auth.uid()` / `AUTH_REQUIRED (28000)`; `clock_timestamp()` → `statement_timestamp()`; the donor's `public.retention_policy_*` tables live in `private` here (no client role can touch them either way); names `rpc_get_retention_policy_status()` (authenticated) and `rpc_publish_retention_policy(text,text,timestamptz,jsonb)` (service_role only, plus the explicit `auth.role()` check); the ambiguity repair (`data_class_code`) is folded in. Added over the donor: `pg_advisory_xact_lock` so a concurrent trusted publication receives `RETENTION_ACTIVE_POLICY_ALREADY_EXISTS` instead of a unique violation, RLS enabled and forced on the inventory table as well, a `required ⇒ active` constraint, and a `RETENTION_POLICY_PUBLISHED` audit row through `private.audit_marketplace` (`SYSTEM`, policy id, null actor).

Contract. Status is fail-closed: `RETENTION_POLICY_NOT_PUBLISHED` until one active policy exists, `RETENTION_POLICY_AMBIGUOUS` if more than one, `RETENTION_POLICY_INCOMPLETE` with `missingDataClasses` until every active required class has a rule; coverage is recomputed on every read. `executionAdmitted` is always `false`: this unit never deletes anything. Publication is atomic — an invalid class, a rule failing a constraint, a duplicate class or missing coverage rolls the whole version back. Retirement stays a trusted SQL act.

MISSING_APPROVED_CONTENT. No retention period, deletion trigger, exception, legal basis or counsel reference exists in any authority document available to this unit; the proof publishes clearly labeled `PROOF` fixture rules. OWNER DECISION REQUIRED: supply the counsel-approved schedule for the fourteen classes (and a purge policy before any execution unit is even planned).

Client. `src/contracts/retentionPolicy.ts` and `src/data/retentionPolicyClientService.ts` are the single client reader: a partial or malformed schedule never reads as ready and no rule is invented. No screen binds it yet (S07 privacy rows are gated on the design track).

Proof. `supabase/proofs/legal/p3_retention_schedule_proof.mjs` on the runner: historical79 → N07/N08 → exact frozen D03 and AI DRAFT to live87 → this file as 88. Eight checks: predecessor without retention objects; exact apply with unchanged history, the exact fourteen classes, empty policy tables, table ACL, function grants and no purge function; fail-closed status for authenticated with anon and service refused; publication trusted-only with every validation code, constraint and duplicate exercised and atomic rollback; complete fixture publication making status ready with the audit row; second active publication refused, retirement reopening, a later-admitted required class reopening coverage and the `required ⇒ active` constraint; one observed publish lock interleaving converging on one active policy; final fingerprints, history and no real content. Local: migration integrity `pending=1`, MD5 manifest, Node syntax, admission tests, TypeScript and jest recorded in the PR.

Limits retained: no UI; no legal content; no purge or deletion execution; no retire RPC; no device proof.
