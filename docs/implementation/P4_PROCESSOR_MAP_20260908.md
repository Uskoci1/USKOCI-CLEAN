# P4 processor/subprocessor map registry — 2026-09-08

Status: SOURCE IMPLEMENTED / LOCAL ADMISSION PASS / DISPOSABLE PROOF PENDING ON RUNNER / NOT LIVE / LEGAL MAP MISSING_APPROVED_CONTENT.

CLEAN has no server-owned inventory of the providers that can process personal data and no versioned, counsel-referenced processor map behind the privacy notice. The donor closed the technical foundation in `rc2_025_versioned_processor_subprocessor_map_foundation` (281 lines); the master's donor→CLEAN reconciliation marks it ADAPT_HIGH because the donor seeds its own project's evidence. This unit ports the design as one forward-only migration: three private tables, two RPCs, no change to any existing object.

Physically verified CLEAN inventory (the only seeded rows). `SUPABASE_PLATFORM` — live project `leqcwgzvjsxugfgzdmth`, Postgres 17, Auth, PostgREST, one private Storage bucket `profile-media`, the deployed `uskoci-ai-interview` Edge function; required and active. `OPENAI_AI` and `GOOGLE_GEMINI_AI` — both adapters are present in `supabase/functions/uskoci-ai-interview/index.ts` (`callOpenAI` → `api.openai.com/v1/responses`, `callGemini` → `generativelanguage.googleapis.com`), the runtime route is decided by the `AI_PROVIDER`, `GEMINI_*` and `OPENAI_*` secrets that this unit cannot read, and the live database holds 22 conversations and 40 messages with zero assistant messages as of today; both are therefore seeded as required-current fail-safe (over-coverage is safe, under-coverage is not). `EXPO_PUSH` — the N06/N07 registry tables exist but nothing in this repository calls `exp.host`; inactive and not required. Not seeded: any map provider (OWNER DECISION on Google Maps vs MapLibre is still open), EAS build (processes binaries and developer credentials, not end-user data), and every donor-only surface (`wjxilkkyyuxyzbvhgmop`, `uskoci-ai-transcribe`, `uskoci-push-dispatch`).

Rebinding. `private.r24_require_user()` → inline `auth.uid()` / `AUTH_REQUIRED (28000)`; `clock_timestamp()` → `statement_timestamp()`; names `rpc_get_processor_map_status()` (authenticated) and `rpc_publish_processor_map(text,text,timestamptz,jsonb)` (service_role only, plus the explicit `auth.role()` check). Added over the donor: a `pg_advisory_xact_lock` so a concurrent trusted publication receives the explicit `PROCESSOR_MAP_ACTIVE_ALREADY_EXISTS` refusal instead of a unique violation, RLS enabled and forced on the inventory table as well, and a `PROCESSOR_MAP_PUBLISHED` audit row through `private.audit_marketplace` (`SYSTEM`, map id, null actor).

Contract. Status is fail-closed: `PROCESSOR_MAP_NOT_PUBLISHED` until one active map exists, `PROCESSOR_MAP_AMBIGUOUS` if more than one, `PROCESSOR_MAP_INCOMPLETE` with `missingProviders` until every active required provider is covered; coverage is recomputed on every read, so activating a provider later reopens the map. `runtimeProviderGateAdmitted` is always `false`: this unit never gates provider calls. Publication is atomic — any invalid entry, unknown or inactive provider, or missing coverage rolls the whole version back. Retirement stays a trusted SQL act (`retired_at`).

MISSING_APPROVED_CONTENT. No legal entity name, role, purpose, data category, region, transfer mechanism, DPA reference, privacy-notice URL, retention term, subprocessor term or legal basis exists in any authority document available to this unit; the proof publishes clearly labeled fixture rows under `https://proof.invalid/`. OWNER DECISION REQUIRED: (1) supply the counsel-approved map for Supabase, OpenAI and Google, or confirm which AI adapter is live so the other can be retired by trusted operations; (2) confirm the map provider before it can enter the inventory.

Client. `src/contracts/processorMap.ts` and `src/data/processorMapClientService.ts` are the single client reader: a partial or malformed map never reads as ready and no provider is invented. No screen binds it yet (S07 privacy rows are gated on the design track).

Proof. `supabase/proofs/legal/p4_processor_map_proof.mjs` on the runner: historical79 → N07/N08 → exact frozen D03 and AI DRAFT to live87 → this file as 88. Eight checks: predecessor without processor objects; exact apply with unchanged history, the exact four inventory rows, empty map tables, table ACL and function grants; fail-closed status for authenticated with anon and service refused; publication trusted-only with every validation code exercised and atomic rollback; complete fixture publication making status ready with the audit row; second active publication refused, retirement reopening, and a later-activated provider reopening coverage; one observed publish lock interleaving converging on one active map; final fingerprints, history and no real content. Local: migration integrity `pending=1`, MD5 manifest, Node syntax, admission tests, TypeScript and jest recorded in the PR.

Limits retained: no UI; no legal content; no runtime provider gate; no retire RPC; no device proof.
