# Public task material — source implementation, physical proof pending

The public task detail now shows the actual description, category, complete public time window, price/capacity, route or work area, skills, tools, vehicles, licences, experience and critical conditions. Requester intent has a guarded Profile path explaining how to switch to JA MOGU. Worker composition remains the existing W05 flow. This is functional layout work; the current owner Figma file returned only cover66:17/66:18 and final screen design is still unresolved. The original supplied logo/intro assets remain preserved.

The typed detail contract crosses the existing discovery service. One explicit public needs read uses LEFT one-to-one child projections so a permitted parent remains readable when child RLS hides material. Missing child data is explicitly unavailable, distinct from a readable empty conditions array. Private addresses, access notes, SDK writes, provider secrets and eligibility verdicts do not enter this presentation. Existing account/session/intent/focus ownership, current-read request guard, error/cache/retry/Back and deadline-driven CTA behavior remain intact. Independent manual review and AST96/39/0 passed.

Source validation: full74 suites/818 Jest tests; TypeScript;67 Python input/observation tests;36 Node local-target/fixture guards; unchanged87/87/0 recorded migration provenance. The full application suite predates only the added proof helpers and documentation merge; application blobs remain identical. Evidence is in [source validation](evidence/public-task-material-20260908/source/validation.json).

A dedicated standalone Android workflow retains all43 original account/Inbox/navigation/recovery pairs and five exact REST stop/restore cycles. It then creates declared local fixtures, executes the actual SDK with owner/worker/outsider/anon identities, checks public/private RLS, and freezes every public/private table row plus schema/history/gates. The new native driver opens real cards and records the fully visible description, route, time, price, capacity and requirements through actual scrolling in two Auth sessions. It uses UI logout/login without clearing between actors, actual Back and Profile controls; no new application is submitted by this slice. Original XML clipping is recomputed, and source/APK hashes and explicit generated build overrides are checked. **This runtime/native execution is pending, not proven by helper tests.**

The separate participant setup uses genuine submit/select RPCs and reaches ACTIVE Need / CONFIRMED Agreement with closed application entry. Its parent-visible/child-unavailable behavior is SDK-only; COMPLETED lifecycle/native closure is not claimed. The disposable database is labelled historical79 plus explicit N02/N03, not full87 replay. The older server geography validator still admits malformed null waypoints/coerced point values; the new strict reader rejects them. Repairing that upstream acceptance is a separate forward unit.

Canonical PR66 is38e9a38 with actual canonical PREP434166399849(64/600,TSC,87/87/0), CodeQL34166399632 and three zero-result/error/warning analyses1737938042/1737937062/1737937020; CONTROL034166399987 proves runner health only. This branch composes PR67ed0 source, whose Android acceptance remains open. No production, provider, final Figma, push/payment/release or overall47-item closure is claimed.
