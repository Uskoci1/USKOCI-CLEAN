# RU-4 — R04 owner edit reaches the live authority — 2026-09-08

Status: SOURCE IMPLEMENTED / LOCAL TYPE AND UNIT PASS / CANONICAL INTEGRATION PENDING / NO SERVER CHANGE.

R04 showed "Izmeni Zadatak" and pushed `/potrebe/[id]/izmeni`, a route that does not exist; the `as any` cast hid it from TypeScript. The 06.09 screen contract lists "Dead edit route" under what R04 must not do, and RU-4 has been CLOSED / LIVE on the server since 05.09 with three authenticated RPCs the client never called: `rpc_ai_open_need_edit_conversation_v2`, `rpc_confirm_need_edit_from_review_v2` and its delegate. This unit binds the existing screens to that authority; it adds no server object and changes no server body.

Flow. R04 states the consequence before anything is touched — the Zadatak temporarily stops receiving new Prijave, passes re-admission after saving, existing Prijave will need a refresh, existing Dogovori do not change — then calls `openEditConversation(needId)`. The server seeds a NEED_INTAKE conversation bound to the Zadatak with its current confirmed facts and returns the exact revision; it refuses after the first Dogovor (`NEED_EDIT_LOCKED_AFTER_FIRST_DOGOVOR`), outside PUBLISHED/SELECTION, or when public geography is not ready. R02 resumes that conversation unchanged and its review CTA reads "Pregledajte izmene". R07 distinguishes an edit conversation from a saved-DRAFT conversation by the bound Zadatak's own state, not by a route parameter: a bound `NACRT` is the existing "Nacrt je već sačuvan" replay, anything else is edit mode. Edit mode re-reads the Zadatak revision at the moment of confirmation and calls `confirmEdit(needId, revision, conversationId, requestId)` with one stable request id for the screen lifetime; the server hashes need, revision, conversation and confirmed facts, so a retry of the same intent replays the receipt and a changed intent is `IDEMPOTENCY_KEY_REUSED`. A moved revision is `STALE_REVIEW_REQUIRED` and never a silent overwrite. Success returns the Zadatak to DRAFT with `requiresReadmission:true` and navigates to R04.

Product language. Server exception names are mapped once in `aiNeedV2Production` (`NEED_EDIT_COPY`); the user never sees `NEED_EDIT_*`, `40001` or `P0001`. Copy uses Vi form and states consequences plainly.

Files: `src/contracts/aiNeedV2.ts` (two result types), `src/data/aiNeedV2Production.ts` (`openEditConversation`, `confirmEdit`, `editFailure`), `src/app/(app)/potrebe/[id]/pregled.tsx` (consequence sheet + real edit entry, dead route removed), `src/app/(app)/nova.tsx` (edit-aware review CTA label), `src/app/(app)/pregled-nacrta.tsx` (edit mode card and confirmation), `src/data/__tests__/ru4-need-edit-client.test.ts` (source guard against the dead route, RPC argument contracts, product-language error mapping, replay receipt, stale re-read).

Not claimed: no native/device proof of the edit journey; no change to publish, which remains fail-closed behind D-0140; no change to the stale-Application resolution that W06 already exposes. The R04 visibility rule (no covered slots, search not closed, not draft/closed) is unchanged and matches the server's lock exactly, so the entry never appears where the server would refuse.
