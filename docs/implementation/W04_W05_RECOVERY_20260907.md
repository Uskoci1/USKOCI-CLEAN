# W04/W05 recovery — integrated source and native proof preparation

Date:2026-09-07. Branch `fix/task-detail-recovery-20260907`; fresh canonical `7c83a1bf09faad1edb7fd0bc2e9199bee645dd41`. This continues the reviewed unit at `5e2831e889fe9a60f1f86d722ae021ae6c5837c4`, with its prior Auth integration `03cecaf28e8766515745042e618937b1aab8bd5a`. No restart, backend replacement or final visual redesign.

## Behavior and authority boundary

The previous W04 could stay loading forever after a successful absent read, turn transport errors into absence and keep old data/actions across route changes. The reviewed source distinguishes loading/error/unavailable/current detail, provides accessible `Nazad na Zadatke` with real-history Back and discovery fallback, and serializes recovery/navigation. Display-only public cache is explicitly marked after transport failure, cannot offer Application entry, and is evicted after successful absence or route/account/session/intent change.

The data adapter propagates errors and returns null only for successful absence. Its17-column public selection retains the existing16 public columns and adds the existing server-owned public `response_deadline`. Counts and deadline syntax must be valid; no default capacity invention. The projection's read gate and W04 deadline timer only suppress navigation; existing database admission remains authoritative. Saved actions are checked again before navigation. The existing sessionEpoch rejects batched A→B→A and conservatively rereads after same-account token refresh; refresh cache retention is not claimed. W05 adds necessary read error/retry/Back compatibility without modifying its existing submit payload, semantic key or server engine.

No direct presentation SDK/network/secret access was added. Static AST evidence has64 client source/25 presentation files and0 findings; this is bounded static evidence, not a complete domain-authority audit. Broader W05 submit lifecycle/readiness handling, hard calendar conflicts and full W04 orchestrated application routing remain separate work.

## Preservation and local verification

The canonical integration had zero conflicts. At integration all16 original W04 unit blobs equalled03cecaf; the four production files still equal reviewed5e2831. A later root-identified native infrastructure mismatch changes only the local REST image guard and its tests: actual Supabase CLI image `ghcr.io/supabase/postgrest:v16.1` is now admitted by its exact namespace. Five spoofed registry/namespace/repository/path variants remain rejected. The same immutable container id, explicit local socket, loopback/project checks and five-outage restoration requirements are preserved. All559 canonical files outside this unit matched canonical before the additive continuity/evidence update. All SQL source, function code, frozen governance, provider configuration and application identity are retained from canonical. Evidence records Git blob ids and file hashes; its dirty-source state accurately identifies the uncommitted merge at the time of scanning.

- Full Jest:52 suites/452 tests PASS; TypeScript PASS.
- Migration integrity:87 files/live snapshot87/pending0; participant contract PASS. No new live database observation was made by this unit.
- Python input/navigation/recovery/local REST guards:45 tests PASS; Node fixture/local-target guards:22 tests PASS.
- Native Android execution and original43 PNG/XML pairs remain **PENDING**, together with final exact-source PR/CI/CodeQL. No accepted earlier Android artifact is relabelled for this source.

## Native replay boundary

Workflow `.github/workflows/task-detail-mobile-proof.yml` runs on this exact feature branch push or workflow_dispatch. It reconstructs the existing guarded historical79+N02/N03 disposable database, runs10 original Inbox checkpoints plus17 navigation/account checkpoints, then16 actual W04/W05 recovery checkpoints. It uses real local Auth/UI actions, not session injection or a production fixture. This is a labelled historical native dependency environment, not full87 database replay or provider proof.

Five bounded outages stop and restore only one immutable local PostgREST container on the explicit local Unix Docker socket. They prove cold error/Back, foreground stale cache without CTA, retry recovery, W05 read error/Back/retry without submit, and a server-owned public deadline closing the CTA while REST remains unavailable. One labelled local deadline Need is armed only after previous journeys. No Application/Selection/Agreement is created for the navigation/deadline fixtures; original Inbox/Agreement/business/0RSD checks remain required.

Dispatch after root review: `gh workflow run task-detail-mobile-proof.yml --repo Uskoci1/USKOCI-CLEAN --ref fix/task-detail-recovery-20260907`. Artifact: `TASK-DETAIL-PHYSICAL-ANDROID-EVIDENCE`, including43 original PNG/XML pairs, proof logs, source/build hashes and separate deadline fixture. Read the original artifacts before acceptance; physical failures remain actionable evidence.

## Historical supplied Edge v8 promotion — 14:26 UTC

At14:26 UTC the root promoted the existing Edge v7→v8 after accepted PR62 actual-handler source proof34131911410 at49fe98a and fresh canonical7c83 gates. The deployed27,775-byte handler SHA-256 is `4421fd3fe16399bfcc6e4820218f080008f6eefd09784b2b08039fe267f3726c`; the unchanged4,097-byte registry is `382bd2c78cdc340207f6986eb2dccb4ca15a94ab3aadd3a1e230a8c9053fcd79`. Both live files were exact at that promotion, with ACTIVE/JWT true. The old v7 source and history remain preserved separately. Secrets were unchanged and OpenAI was not configured at the14:26 promotion boundary; those historical facts must not be presented as current configuration after the owner's later setup. Actual provider success remains unproven. The44 tests use mocked environment/transport; they are not a real provider interview. No live call/deployment belongs to this W04 integration.

Evidence: [manifest](evidence/task-detail-recovery-20260907/SHA256_MANIFEST.json), [source preservation](evidence/task-detail-recovery-20260907/source-preservation.json), [client AST boundary](evidence/task-detail-recovery-20260907/client-boundary.json), [root Edge v8 promotion](evidence/task-detail-recovery-20260907/edge-v8-selector-promotion.json).

## Later owner configuration — separate14:44 observation

The owner subsequently saved provider configuration at14:38–14:42. Root's names-only metadata at14:41:53 observes all three required entries. `AI_PROVIDER=openai` and `OPENAI_MODEL=gpt-5-mini` are user-confirmed manual saves; their values were not independently read back. No API key value was read or persisted in this evidence. The agent's configuration-write attempt was rejected for insufficient privileges; the owner performed the saves.

The root's14:44 Edge metadata is version11, ACTIVE/JWT true, with the same EZBR and two exact canonical source files as the historical v8 promotion. This is configuration/source evidence, not successful provider execution. The owner walkthrough outcome remains pending. See [sanitized configuration metadata](evidence/task-detail-recovery-20260907/openai-configuration-metadata.json). The earlier v8 record is retained unchanged and describes only its14:26 boundary.
