# CDL-A12 proof expectations

1. Prove current spread precedence while both implementations exist.
2. Prove canonical winner resolves authenticated requester profile.
3. Prove exact `rpc_ai_publish_need(p_conversation_id,p_profile_id)` invocation.
4. Prove server failure remains failure (`PACKAGE_4_NOT_READY`), never fake success.
5. Delete only lower stale publish shadow.
6. Make surviving owner structurally required.
7. Run migration integrity, TypeScript and full regression before promotion.
8. Re-read canonical GitHub and live Supabase after promotion.
