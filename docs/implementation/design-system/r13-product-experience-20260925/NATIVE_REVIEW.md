# R13 native review — bounded scope

Ordinary system text is the design baseline. Enlarged-text checks only look for lost content/actions; they do not
require large ordinary titles or prices. Physical-phone acceptance and real business journeys remain open.

## Initial APK: be72399d

Run 36135521690, APK SHA-256 `e17d2c7cab6649f512c71fec8bb499acc23cbd5747c258a9d5299d91df430a64`.
Both source/tree/run/hash attestations and x86_64 payload passed. Installed on emulator-5554 with `adb install -r`;
no data reset. Fourteen capture records are kept under the outer workspace's `outputs/r6-integration/r13-experience/`.

| Surface | Observation | Limit or refinement |
| --- | --- | --- |
| Home and first entry | Both starting actions, attention and next appointment readable at ordinary size. | Inert same-presentation gallery; its frame intentionally omits root navigation. |
| Discovery and selected pin | Real seven-result list, four tasks without map points; readable new card/preview and selected root tab. | Selected Belgrade job still shows a regional map: correct explicit-selection zoom. No publication/application command. |
| Public task detail | Work, truthful price wording, paired time/people, 56 dp publisher fallback and footer visible. Lower scroll reaches approximate area, attribution and questions. | Read-only existing task; media upload, owner mutation and application submission not exercised. |
| Search | White sections, readable selections, real-result action. Three-person filter previews and applies two results. | Client search only; no alert created. |
| Offer, review, uncertain and confirmed receipt | Shared terms include the entire submitted note; existing send/edit/recovery/result actions reachable. | Inert commands. Normal 32 sp amount is too strong; refine to 28 sp. Review XML timed out: screenshot evidence only for that capture. |
| Two-digit people input | At 320 dp/font 2, typed 10 fits and the existing capacity refusal blocks review. | Brief resilience inspection only. Returned emulator to density 420/font 1.0 afterward. |

No paid provider, microphone, key, new account, live business command or database mutation was used. Real task
screenshots remain local. This pass does not establish Android/iOS performance, screen-reader flow or a real
two-person task-to-rating journey.

## Refinement APK

The correction tones down amount typography and adds an explicit pin/place neighborhood frame using only the
existing rounded public point. Exact source, checks, APK and fresh normal-size observations are recorded in the
batch receipt when complete; initial images must not be presented as proof of these corrections.
