# Next source batch — Home, Agreements and conversation

Preparation only, against the R16 source. No code or device acceptance is claimed here.
DD-06–DD-09 are still open; DD-10 has only the root bar's shadow refinement.

Carry the bounded R16 native findings into this batch: stack the people-filter group at the owner's 361 dp / 1.15
without enlarging type; evaluate the card-foot breakpoint with long identities; inspect return/snap camera framing
and the fallback ring behind rich markers. The first post-install deep link recovered after one retry: reproduce
and classify it before calling it a proven session defect. Do not change server/auth behavior speculatively.

1. Agreements: work title beside the existing portrait; name/role supporting it. Remove the decorative appointment
   rail and its cumulative 54 dp inset; keep accepted time/place/people/total complete. Preserve next-action priority,
   independent rating target, honest missing facts, problems and pending changes. Remove tab-edge fading.
2. Segmented underline: reuse the measured indicator, native translation and reduced-motion handling already used
   by the filled variant. Keep immediate selected semantics, first-frame fallback and label/count remeasurement.
3. Home: retain both existing illustrated starts and all four actual attention reasons. Upcoming work becomes one
   clean appointment without simultaneous outline, divider and green rail. The Home projection has no portrait ID,
   price or location: do not invent these. Keep partial reads, legacy `row.detail` display when appointment fields
   are absent, and actual rating counts. Never restore client-derived attention after a failed production attention RPC.
4. AI draft: local expand/collapse is separate from the guarded review command. In the worker flow review calls
   `api.prepare()`; a disclosure must never invoke it. Scope disclosure to conversation identity, keep safety notes
   visible, preserve category masking, missing-map prompts and complete frozen review. Preserve account/revision
   route keys and the stable task `conversationKey`, including its pre-conversation/open-request identity. Disclosure
   and guarded review are separate sibling targets; do not leave the entire draft card as the review target.
5. Conversation: quieter assistant reading surface, consecutive speaker label once but per-turn accessibility
   retained; one composer edge. Preserve completed-row announcements, streaming/history gates and every recovery
   action. Keep ordinary text and 48 dp controls.
6. R14-N01: AI and human chat need a stable follow-latest intent unaffected by geometry-driven scroll events.
   User scrolling and explicit send/latest commands decide that intent. Keyboard changes affect both viewport and
   context/history height; do not close the human-chat finding with an AI-only change. History readers retain place.

Existing relevant files: `HomePresentation`, `AgreementCollectionPresentation`, `Segmented`, `AiConversationShell`,
`IntakePresentation`, `WorkerAiPresentation`, `AgreementThreadPresentation`, `AgreementChat`.

Use the existing focused suites for disclosure-without-command, guarded review, unknown outcomes, first pending
turn, layout while following latest versus reading history, photo recovery and tab semantics; then combined types
and Jest. One coherent build checkpoint, native ordinary and narrow/large-text checks. No provider/prompt, microphone,
backend, payment or dependency change is implied.
