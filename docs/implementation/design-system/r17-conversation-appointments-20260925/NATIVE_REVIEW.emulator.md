# R17 emulator native review — 2026-09-25

Source `8500bf29db718230c8706a39609e3f73a6373b59`, tree `463ee4b36c605a1ced36205388ea67128b8eafc8`, CI run `36172928504`. Installed APK SHA-256 `84e2069495ccb90973babc150c1d25bdfe73053fe23b8cf3700872ad4d8e87d2`; receipt records successful installation on `emulator-5554` at `2026-09-25T18:37:26.795132+00:00`. Review used that existing receipt and `r17_capture.py`; no reinstall or source changes.

Baseline: 1080×2424, physical density 420 (~411dp), font 1.0. Resilience check: density 540 (320dp), font 2.0. A guarded process restored density reset/font 1.0 in `finally`; final adb read confirmed physical density 420 and font 1.0. Emulator ends on the inert worker AI fixture. Phone was never controlled.

All 21 retained PNGs were actually viewed; current hashes match CAPTURES.json. Five records are diagnostic only, two further expanded AI records have explicit limits. Menu/IME diagnostics are not product acceptance. The misleading intermediate name `chat-normal-docked` was renamed `chat-normal-floating-ime`; it never demonstrated a docked keyboard.

## Observed

- **Normal Home and Agreements:** `home-upcoming-normal.png` shows the whole next appointment: task, exact date/time, person and role, with clear surrounding actions. `agreements-list-normal.png` retains person/role/status/date/place/price and separate next actions in open rows. Their hierarchy is readable without nested panels. These fixtures lack real bottom navigation; their safe-area/nav composition does not establish production navigation behavior.
- **Normal AI:** `ai-task-normal.png` and `ai-worker-normal.png` show compact summaries with distinct disclosure and review actions, legible conversation and reachable composer. Task missing fields and price basis remain explicit. The initial worker capture appeared to lack its down-caret; one fresh same-screen capture clearly shows it. The current file/hash is that fresh image. Cause of the earlier appearance is unproved; no runtime edit was made.
- **Narrow AI:** task/worker disclosure captures prove the inline summaries can be reached by scrolling to the conversation start. Collapsed two-line titles truncate intentionally; task missing fields/amount/review and worker review are readable. Expansion exposes full title/skills and existing facts. The latest button remains available. At this offset it overlaps the lower expanded context/review, so additional scrolling is necessary; expanded review reachability was not independently completed.
- **Human chat:** `chat-normal.png` shows the latest failed message, honest failure copy/retry and composer. A deliberate older-history swipe exposes the oldest fixture messages plus “Najnovije poruke” (`chat-older-normal.png`). `chat-media-normal.png` shows pending photograph uncertainty and reachable remove/check-and-retry controls. No retry/send/photo action was invoked. The narrow `chat-media-320-font2.png` has the media tray closed after configuration change: it proves the compact header, failed-message retry and composer, not narrow photo recovery.
- **Filter refinement:** actual Discovery loaded 9 tasks/4 without a point and a map cluster without a retry in this run. `filters-people-320-font2.png` shows “Koliko vas dolazi” on its own full-width row above the stepper, its explanation, price options, and stacked footer. Count 9 agrees with Discovery. No value change, reset or Apply was used; panel was dismissed. This 320dp/font 2 check supports the stack refinement but does not replace the exact 361dp/font 1.15 phone check.

## Open evidence and criticism

**R14-N01 remains open:** the emulator used a floating handwriting/physical-keyboard toolbar. Its visible “Show on-screen keyboard” command produced a floating Gboard panel; dragging did not dock it. `show_ime_with_hard_keyboard` was already 1. These bounded attempts were stopped without changing IME configuration. Toolbar opening leaves app geometry unchanged, so it cannot prove follow-latest, older-reading anchor, keyboard close restoration, or disclosure readability under a real docked resize. Floating overlays obscure the left edge of the expanded AI captures; do not label these passing keyboard evidence.

At 320dp/font 2, the large latest-message pill overlays lower AI content while reading earlier; the expanded task review is partly behind it and worker review is below the viewport. This is a concrete friction observation, not proof of permanently unreachable content. Normal-size composition is substantially clearer. Discovery quick chips and attribution are horizontally clipped/scrollable at this extreme scale; no attribution removal was observed. Map marker fallback-ring and return-camera refinements were not exercised by this pass.

## Boundaries

Home/Agreement/AI routes use existing inert fixture props and no-op business callbacks. No provider, microphone, send, save, agreement action, gallery or camera command was invoked. The actual `/zadaci` visit was read-only. No account/reset/uninstall/backend or repository action occurred. Fixture isolation does not assert zero app-wide background network traffic or prove server command behavior. All source-bound details and individual qualifications are in `APK.json` and `CAPTURES.json`.
