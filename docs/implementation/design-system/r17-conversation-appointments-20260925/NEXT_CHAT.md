# Next human-chat package — bounded diagnosis

2026-09-25 · source `8500bf29` · read-only local inspection; no DEV query, runtime edit or new test run. Paths below are relative to `work/uskoci-r6-integration`.

| Area | Current evidence | Boundary / next change |
| --- | --- | --- |
| Incoming refresh | `src/hooks/useFocusedResource.ts:15` refreshes on route focus/app activation; `src/app/dogovor/[id].tsx:165` loads messages even on the overview tab. `src/ui/AgreementChat.tsx:162,213,235,297` refreshes after send/retry and manually. There is no timer/subscription in this path. | A foreground, focused-Poruke-only, single-flight refresh controller is client work. Existing `messages.refresh` uses the default **load** mode, clearing history; `src/data/focusedResource.ts:32` already supports `keep` during loading and `silent` through failure. Pass the separate refreshing state to the pull control. Avoid repeated full-history polling as the long-term delivery mechanism. |
| Paging / cost | `src/data/supabaseIzvor.ts:298` reads ascending `(created_at,id)` with no explicit limit/cursor; `src/data/ports.ts:71` returns only an array. `src/contracts/projections.ts:465` retains display time, not the raw cursor. Every refresh also reads photo metadata in batches of 50 (`src/data/agreementPhotoClientService.ts:106`). | A bounded latest page plus older-page loading can use the existing authorized table read: retain exact raw timestamp + ID, request one extra row for `hasMore`, deduplicate by ID and preserve the visible anchor on prepend. This needs a client contract/controller change, not necessarily a new RPC. Current reads cannot prove completeness beyond an external API row cap. Do not manufacture a cursor from formatted time. |
| Read boundary | The loaded Poruke list triggers agreement-wide notification settlement (`src/app/dogovor/[id].tsx:192`). Candidate `supabase/candidates/pkg050a_agreement_messages_read.sql:54,70` accepts only Agreement ID and settles all eligible caller-owned message events; it has no displayed-message/high-water argument. Tests explicitly pin this behavior (`src/data/__tests__/pkg011-agreement-workspace.test.tsx:338`). | Viewport gating is client work, but cannot prevent a message arriving between read and acknowledgement from being settled unseen. A bounded read-through-message/event receipt requires an additive server contract, participant/actor validation and concurrent-arrival proof. Page-size reduction must be coordinated with this change; retaining the blanket acknowledgement would mark unseen older pages. `procitano: null` remains truthful; notification settlement is not a counterparty read receipt. |
| Outbox | `src/hooks/useAgreementOutbox.ts:14` binds account + Agreement + account revision. `src/data/agreementOutbox.ts:215,253,273` persists before send, retries the same immutable command and reconciles exact sender/key/body/photo evidence. Route reconciliation repeats after hydration (`src/app/dogovor/[id].tsx:184`). | Preserve this authority. A row absent from a page is **unknown**, not failed/unsent. A confirmed local row outside the loaded page is not automatically a duplicate. Keep same-key retry and account fences; do not reconstruct history from the bounded confirmed cache. Lost-ACK, hydration and late-timeout tests already exist in `agreement-outbox.test.ts:28,253,271` and `agreement-screen-recovery.test.tsx:158`. |

**First implementable package: non-destructive, coalesced human-chat refresh.** Adapt only this chat's manual/send/retry refresh calls to retain the displayed transcript, expose refreshing separately, and serialize overlapping refresh requests without changing the shared resource's defaults. Preserve background forgetting, account-revision fences, draft/photo recovery, scroll intent and existing acknowledgement semantics. Test overlapping send/manual refresh, late old-account results, history position, failed-refresh copy, and lost-ACK reconciliation. This improves the present experience without pretending to solve continuous incoming delivery or exact read boundaries. Follow with bounded paging + read-boundary contract together; a subscription requires separately verified authorization/configuration and is not evidenced here.
