# Continue after R19 without repeating the audit

Latest update: NATIVE_REVIEW.viewport-guard records b5a82f69's retained-but-unapplied scroll failure and bounded
native 1/1,000-row gallery results. CHECKS.explicit-viewport records the subsequent actual-height correction.
Verify its next APK with real and deep local-gallery detail/Back before calling scrolling closed. Phone is back;
the prior phone run36226688621 was cancelled before installation. No new backend or provider authorization.

Latest continuation, 26 September: read CLIENT_GUARDS_AND_VIEWPORT and NATIVE_REVIEW.vector-diagnostic first.
The exact ba3f7dc3 APK now proves visible vector logo return and selection success footer (normal/1.3 font, inert
gallery). Scroll's measured cause is a transient content-sized viewport. The subsequent source rejects impossible
geometry; native replay is still required. Block consequence confirmation and expected-profile avatar discard
receipt binding are implemented client-side; trusted safety names and unknown upload recovery remain open.
The DEV-only dizajn-mapa route has 1/1,000 local rows, not a server load test. Check its later APK receipt before
claiming any device result. Keep the current source/native distinction rather than restarting earlier attempts.

First R19 runtime source: `3cdb3005c393c2a47572452d3704c69de9799d25`, tree `1fa68354519fefc4bcfa6e1428433ab786bc6951`. A corrective native follow-up retires old sheet callbacks and restores the sheet on focus re-entry; its exact later source/install belongs in RECEIPT.json.
Read REPORT, CHECKS, RECEIPT and native evidence first. This document orders work; the living tracker remains `docs/control/redovi.json`.

## Preserve what was proved

- R18's exact two-account, one-person, remote, offers journey reached both saved ratings. Do not turn it back into an unknown result or extrapolate it to other scenarios.
- R19 fixes binary image reading, optional author fan-out, safety-entry retirement and Inbox duplication, as well as task-led Agreements, photos and map composition. No backend deployment belongs to R19.
- The first integration passed 320 suites / 6,251 tests. The corrective follow-up passes 320 suites / 6,255 tests plus clean types. Its passing status is not native or store acceptance.
- The 1,000-task fixture proves geographic membership, remote independence and counts locally. The 500-completed-Agreement test proves a bounded review pool. Neither measures server load, frame time or 1,000 simultaneous people.
- The DEV observation is ledger 202. The fresh private certificate query was denied; the September 24 control snapshot is historical, not silently refreshed by generating the dashboard.

## Next coherent client package

1. R18-E01/E02: distinguish a known, server-confirmed eligibility refusal from an unknown write outcome and expose only allowed blocker reasons with a useful correction path. Preserve the immutable pending-command identity and reconciliation.
2. R18-E03: reconcile location and schedule facts on applications with the authoritative projection; do not invent REMOTE or an end-only date when the server projection does not supply it.
3. Finish safety context and explicit block consequence confirmation, using a verified target binding. The R19 callback fix does not supply a verified display name or a new confirmation screen.
4. Resolve the remaining selected-success visibility and requested-versus-settled sheet state issue from MAP_AUDIT using native evidence. Keep one map, one list and one production composition.
5. Complete avatar unknown-outcome cancellation/recovery separately from successful JPEG reading. A picture rendering is not proof that every interrupted upload can recover.

Prepared bounded continuation, read-only at source `46c87a38`: R18-V02 is the clipped success message, **not** a missing or clipped Open Agreement button. In ApplicationSelectionPresentation the confirmed success row is at the end of the scroll body (331–332), while the button is already in the fixed footer (289/301). Move the single compact success row into that same footer, preserve receipt-derived `confirmed`, `SuccessMark fresh={!confirmedAtMount}`, accepted terms and explicit manual navigation; do not change the route/journal. Extend application-selection-native.test.tsx's existing successful-selection test to assert one confirmation and the button within `product-sheet-footer`; existing unknown-result/account-change tests remain. Native long-offer/font acceptance is separate.

Avatar preparation: fotografija.tsx currently allows discard only for a READY candidate; known PROCESSING/STAGED receipts still show recovery without discard. The checked historical `rpc_discard_profile_avatar` body allows non-applied AVATAR assets irrespective of readiness, but fresh active-body verification is required before extending that UI. Keep an unknown/absent receipt journal intact: TASK-scoped cancelUploadCommand is not an avatar cancellation fence. Any discard receipt must be bound to the expected profile as well as asset/account; currently the decoder requires a valid profile UUID but not the expected profile value. No new avatar mutation or server cancellation package has been implemented by this preparation.

Later R19 vector/diagnostic source implements the selection-success footer move above, with 50 focused tests and
clean types. Its native acceptance is still separate. See VECTOR_AND_DIAGNOSTIC.md and that version's check/APK receipt.

SF-02 preparation: SafetyScreen currently sends setBlock on first press. Reuse useConfirmSheet from profil/blokirani.tsx, but leave editor.save, CAS/revision, command identity and unknown-outcome reconciliation intact. Dismiss on focus/context/read-generation change; cancellation sends nothing. Existing readBlock/readTarget return account identity, not a name. A uniform name read does not exist: Agreement context can use agreementChangeService.read only after correlated participant/target checks; profile context needs both javniProfil(profileId) and readTarget(profileId) checked against the actual target account. Preserve profile context through useSafetyEntry if used, never accept route-provided display text as authority. Do not walk all blocked-list pages to find one name. Direct/unavailable-name entries keep an honest generic context and must not disable reporting. Tests: v5-safety-screen, safety-route-guard, public-profile-safety-entry and safety-authority; blocked-accounts-screen already demonstrates confirmation retirement. This is preparation only, not an implemented or DEV-verified fix.

## Required server design/proof packages — no automatic DEV application

### Guardrails from the final bounded source review

The current `moje-prijave.tsx` already reconciles the named pending row via `readApplicationCommandState(pending.row)` alongside the list; older list-only reconciliation warnings are historical. Preserve that independent row read when introducing paging. For eligibility, `serverReceipt.readOwnedResult` currently retains error.message but discards SQLSTATE/details: matching a human-readable error code alone is not a typed refusal receipt and must not release a pending journal. The inspected atomic-submit SQL replays saved success before eligibility, then raises `WORKER_NOT_ELIGIBLE` with P0001 and bounded blocker details before writes. Start with precise allowed copy/correction links and existing reconciliation; any immediate journal release needs a narrowly validated, account/request-owned definitive refusal contract and fresh authority verification. Timeout, malformed response, missing receipt and reused key remain uncertain. Application location/end-only/timezone gaps need an additive server projection, rather than guessed client facts. Safety confirmation can be client-only, but any displayed name must match authorized profile/Agreement/block-list context, never an untrusted route label.

| Package | Required semantics | Evidence before any apply decision |
| --- | --- | --- |
| Discovery page/count/author projection | Same filters and visible-area meaning, whole-country bounds, separate remote/no-point membership, flexible/end-only/date-overlap semantics, deterministic cursor, no private location leakage | Disposable parity fixtures for 0/1/1,000 rows, stable ordering and cursor boundaries, account/RLS tests, bounded query plans and count agreement. Do not blindly forward client filters into the old 3-by-5-degree bbox contract. |
| Home / Agreements / reviews | Next work and attention/counts without walking all completed work; review status in a bounded projection | Active work preserved with 0/1/1,000 completed rows, all four attention reasons, unavailable versus zero, caller isolation and pending-command reconciliation. |
| Candidate paging and selection | Candidate projection is currently an unpaged aggregate with per-row work; page/count must preserve last-slot and exact accepted terms | Concurrent final-slot selections, changed task/offer revisions, team-slot and both price bases, withdrawn/stale/blocked candidates. Never treat a virtualized FlatList as proof of bounded SQL/network cost. |
| Human chat | Latest/older cursor pages, incoming reconciliation and exact displayed-message acknowledgement | Reordered/duplicate delivery, offline/unknown writes, long histories, account changes and a new unseen message arriving during acknowledgement. Existing notification settlement is not a read receipt. |
| Closure/media recovery | Account-owned recovery without the original device key; correct media cancellation lock ordering | Disposable concurrency/erasure proofs; explicitly seek any protected certificate change. Never delete a real account to test it. |
| Review comment | Define length, audience, reporting/moderation, retention and erasure before adding input | Candidate SQL, clean disposable proof and client contract. No text field that current RPCs cannot save. |

Keep these packages small and separate; do not bundle a visual tweak with new business or retention semantics. Write candidates/proofs first and require the owner's explicit `primeni` before DEV changes. Payment/PKG-051 stays outside this work.

## Native and operational acceptance matrix

- Extend the recorded R18 case with onsite/private address, multiple candidates/participants, final-slot competition, changed terms, cancellation before/after completion, disputes, offline recovery and third-account isolation. Use an approved test scope and disposable databases for destructive/concurrent permutations.
- Verify actual push registration, current opt-in settings, provider delivery, banner/tap/cold-start/account destination. The R19 navigation unit tests do not prove any notification was delivered.
- Worker AI interview → review → activation needs its own authorized account/provider trial; the paid allowance for R18's task is already used. Microphone tests require owner readiness. iOS dictation is currently unavailable; full spoken AI replies remain separately scoped.
- Operator/legal/retention/export, moderation/support ownership, production configuration, signed AAB, iOS/TestFlight, payment and store evidence remain open in the control table. Do not translate a green build into release readiness.

## Cleanup and coordination

Do not restart a whole-repository reread or call all old R6 rows current bugs. Follow the exact coverage sections of the three R19 audit reports. Remove code only after checking imports, routes/deep links, pending journals and proof loaders. Frozen migrations, applied bodies, compatibility endpoints and real DEV records are not cleanup material.

Preserve the exact-source receipt split when later commits only change documentation. Update the control table and run `node scripts/control/osvezi.mjs`; publication to the owner's existing private Artifact is a separate observed result. Never invent successful upload after a chooser timeout.
