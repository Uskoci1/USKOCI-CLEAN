# Intent-shell Android run 34114525948 — scoped physical proof PASS

- Run: https://github.com/Uskoci1/USKOCI-CLEAN/actions/runs/34114525948
- Job `101718141318`, SUCCESS, `2026-09-07T11:02:20Z`–`2026-09-07T11:29:03Z`.
- Exact source: `86fffc1b58789a2a7331e3646ba8aa0647e02a73`.
- Artifact `10016574410`, `INTENT-SHELL-PHYSICAL-ANDROID-EVIDENCE`; original ZIP 4,145,359 bytes.
- API digest and independently computed original ZIP SHA256 match: `c2d067810d728571780ddca423cc09ac1bf7aa9f9a494b1a774e20c4a2e33767`.
- Runner-recorded APK SHA256: `d3c6f3ae7188962159805a458489c88698e61a8d25e08d0f79f964445c2fb46b`.
- Package `rs.uskoci.n04proof`, Android API35 / Pixel6 / x86_64 emulator, real native release APK and UI input against disposable local Supabase only.
- Four executing proof-script SHA256 values independently match the exact Git blobs at this source. Navigation fixture records the same source and `localOnly=true`, `publicationProof=false`.
- Linux: TypeScript PASS; 44 Jest suites / 290 tests PASS; 34 Python tests PASS; local guard tests PASS; native build and local-only bundled-endpoint checks PASS. Source migration integrity: 85 files, live snapshot 85, pending 0. Device fixture deliberately reconstructs local79 plus N02/N03; this is not live85 behavior proof.
- All six exact-source GitHub checks SUCCESS. Three actual CodeQL analyses under `refs/pull/49/head` have zero errors and zero results.

All **27 original PNG/XML pairs** were inspected. Every original PNG was opened individually, and every paired XML was parsed with all text/accessibility labels reviewed. Each original image is 1080×2400. None shows a launcher dialog or app ANR. Original bytes were preserved; viewer resizing is not an artifact modification. `pair-inventory.json` records per-file hashes, dimensions, full labels, actionable bounds and completed review markers. `source-boundary.json` records 109 exact Git blobs and SHA256 values for native sources/config/vendor and executing proof infrastructure.

## Confirmed row correction

Both Agreement lists show the same real `RU5 device journey ac4d2b0b` Agreement with the actual counterpart, `Fleksibilno`, full `3.000 RSD` and the trailing chevron. The worker's long requester name wraps onto a second line without moving schedule or amount offscreen. The requester row has clear spacing as well.

The recorded assertions and an independent replay of the exact-source helper against the original XML both pass:

| Role | Schedule bounds | Full amount bounds |
| --- | --- | --- |
| Requester | `[142,616][311,664]` | `[764,616][931,664]` |
| Worker | `[142,664][311,712]` | `[764,664][931,712]` |

All fields have positive bounds within their labelled actionable card and the screen. Original images independently confirm readable text and spacing. This resolves the concrete row overflow from run34110549395, whose original files and failed visual disposition remain preserved.

## Preserved flow and side effects

The ten original N04 pairs retain requester event/read-all/zero-bell and empty-role behavior, plus worker selection event → real Agreement → 32-to-31 unread transition → pagination to older events 2/1 → all-read. The seventeen NAV pairs show requester `Zadaci / Novi / Dogovori`, real Novi entry, actual own profile and Back, Inbox and Back, same-account JA MOGU discovery/profile, UI logout to the public signed-out landing with sign-in and no private tabs, a second UI login without app clearing/session injection, a fresh second-account requester empty state and own profile, worker discovery → W04 → Android Back, selected application and worker Agreement list. No dummy rating or fake map/Kombinovano mode is shown.

Requester account `71b535e8-7918-4cd6-b30b-e417bc54cc51`; second account `8fede532-5c31-4327-b224-9e7722bc2a26`. Profile identity/name/city assertions match their actual local projections.

**Expected local writes:** tapping Novi invokes the authenticated conversation-open path. The requester's NEED_INTAKE / NEED_FACT_V2 conversation count rises 0→1; no message or publication is submitted. Original N04 setup performs authenticated application/selection and its UI marks activity events read. Final postflight keeps one original response, selection and Agreement plus requester-beneficiary REQUESTER_SELECTION_V1 activation at zero RSD. The additional NAV Need stays PUBLISHED with no application/selection mutation. Push attempts/delivery reads remain zero; publication policy/decisions, Q&A, FASTEST and AUTO_FILL gates remain unchanged. This is not a claim of zero business writes or any production mutation/proof.

The W04 screenshot retains the older empty `Uslovi:` heading and Android Back boundary; a separate W04/W05 unit addresses that surface. This scoped scaffold/navigation proof does not establish final visual design, all product surfaces, publication/provider/push readiness, or behavior of later source changes.

Disposition: **physical navigation/Auth-account/Inbox and corrected Agreement-row proof PASS at exact source86fffc1**. New user steering requires an architecture-boundary audit before any merge. This proof remains valid for its own recorded source; any later Auth/client-boundary source changes require fresh final gates and native proof. No merge approval is implied by this evidence record.
