# Intent-shell/Auth Android run 34119448882 — scoped native proof PASS

Exact source: `29271e6be7fc519e40941531d37c6d48b0db7ef3`. Run [34119448882](https://github.com/Uskoci1/USKOCI-CLEAN/actions/runs/34119448882), job `101733867638`, SUCCESS. This source includes the reviewed central Auth command service and monotonic account revision as well as the corrected Agreement rows.

Original artifact `10018537383`, `INTENT-SHELL-PHYSICAL-ANDROID-EVIDENCE`, is 4,160,062 bytes. Independently computed ZIP SHA-256 matches the API digest: `e87d3f887870505942806faa3824f80c699234835f18e31ca5ebd582f62a1acc`. Runner-recorded APK SHA-256 is `fd6f513be2f8e0fe71fcff4688de5a1db30d3dfdab0d1c55661133758650708d`. All four executing proof-script hashes match the exact Git blobs. Original ZIP, PNG/XML bytes and complete job log are retained; viewer resizing did not alter any artifact.

All **27 original PNG/XML pairs** were inspected. Each PNG was individually opened, each XML was parsed and every text/accessibility label reviewed. Every image is 1080×2400. No checkpoint shows a launcher dialog or app ANR. `pair-inventory.json` records original per-file hashes, full labels/bounds and completed review markers. `source-boundary.json` freezes 138 Git blobs/SHA-256 values covering native source, assets, config/vendor and executing proof infrastructure.

Both real Agreement rows show the full counterpart name, `Fleksibilno`, `3.000 RSD` and chevron. The long requester name wraps without displacing schedule/currency. Independent execution of the exact-source metadata assertion against the original XML passes for both roles:

| Role | Schedule bounds | Full amount bounds |
| --- | --- | --- |
| Requester | `[142,616][311,664]` | `[764,616][931,664]` |
| Worker | `[142,664][311,712]` | `[764,664][931,712]` |

The original N04 sequence retains requester unread/read-all/zero-bell, empty role, worker selection event → real Agreement → 32-to-31 unread transition, pagination to events2/1 and all-read. The additional seventeen NAV checkpoints show both three-zone intents, real own profiles, Back/Inbox, current-account mode switch, UI local logout to the public signed-out landing without private tabs, a second real UI login without clearing app storage, fresh second-account requester empty state and profile, discovery/detail/Back, selected application and both Agreement lists. No fake reputation or combined/map placeholder is shown.

Requester `b9e4d88f-d1ed-43b5-b29b-612c6074f736`, second account `418ae1d1-4386-4e28-b016-3dd98a37b88d`; actual profile projections match their identity/name/city. The selected Need is `RU5 device journey 020d3fc7`, navigation fixture `NAV discovery 8407e240`.

Expected local writes are explicit: Novi opens one owned `NEED_INTAKE` / `NEED_FACT_V2` conversation, count0→1, without sending a message or publishing. Original N04 setup performs authenticated application/selection and UI read-state writes. Postflight preserves one response, selection, Agreement and REQUESTER_SELECTION_V1 activation at zero RSD; the additional NAV Need receives no application/selection. Push attempts and delivery reads remain zero. Publication/Q&A/FASTEST/AUTO_FILL gates remain unchanged. This is not a claim of zero local business writes.

Linux exact-source gates PASS: TypeScript, **46 Jest suites / 313 tests**, **34 Python tests**, fixture guards, migration integrity85/live snapshot85/pending0, native build and local-only bundled endpoint. All six source GitHub checks SUCCESS; three actual CodeQL analyses have zero errors/results. The DB fixture is the recorded **disposable historical79 plus N02/N03**, not live85 behavior. The standalone APK uses Android API35 / Pixel6 / x86_64 emulator and package `rs.uskoci.n04proof`; it is not physical hardware or production evidence.

Disposition: **native shell/navigation/Auth-account/Inbox and corrected Agreement-row proof PASS at exact source29271e6**. Source/component tests separately prove batched A→B→A invalidation and same-account token-refresh preservation; those concurrency scenarios are not represented as additional native observations. The required PR49 source boundary review passed, with AST62 client files /25 presentation files /0 findings for its bounded rules. The separate AI/domain-authority work is not claimed complete by this proof.

W04 still has its older empty `Uslovi:` heading and Android Back behavior; the separate task-detail unit owns that change. Current UI remains a testable scaffold, with future Figma visual authority and separate Fable assets. No final design, complete product, provider/publication/push or Store-readiness claim is made. A documentation-only follow-up may retain this proof only after equality of all138 frozen blobs and fresh final-head CI/CodeQL; no merge is implied by this evidence record.
