# Public evidence copy: c1 Android run 34160744232

The 34 PNG/XML checkpoints, original MP4, decoded PNG frames and original GitHub artifact ZIP are retained with their verified original binary bytes. The ZIP and all 75 expanded members were scanned; no credential match was found inside the ZIP.

All standalone `.log` files in this public directory are **normalized public copies**, including historical filenames containing `original`. Credentials were redacted from `complete-job.log`; ANSI escapes, CRLF and trailing whitespace were removed from public logs. Every public log starts with its original raw SHA256. Original raw logs remain only in the workspace evidence directory and must not be copied into Git.

`public-redaction-report.json` records names/counts and original/public file hashes without credential values. `review-file-manifest.json` and `source-gates/manifest.json` describe the public Git-normalized bytes, not unredacted raw log bytes. The original artifact ZIP digest remains c0932b714237ae92d3ba78fd372d06ce13da5f571e32a7a5aa9b3a3c0b7ff602.

Static checkpoint review is scoped PASS (34/34). Full visible 4500 ms motion is NOT accepted: the original video shows the incoming Auth route slide hiding the beginning. See `motion-review.json` and original-PTS frames 55, 56 and 68. This archive does not prove provider calls, publication, production changes, payment, push delivery, recovery completion or the entire product.
