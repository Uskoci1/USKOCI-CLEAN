# Public evidence copy: run 34163510260

Original standalone raw logs remain outside Git. All public .log files, including historical names containing original, are redacted/normalized copies with raw SHA256 headers. PNG/XML/MP4/ZIP bytes and decoded PNG frames remain original.

The review manifest describes public Git-normalized bytes. Read the scoped visual/motion review; a successful workflow alone does not establish complete visual or product acceptance.
