# PR64 original native acceptance review

Source `3f73321927ff5a0d01eb4b884c347d73a30258a3`; run34159176253; artifact10032628812; original ZIP SHA-256 `cca131ad9a0dfb86e7a7f4be16232ab981b9caceb0f449fe1b0e58f909b13ab9`. Original download digest, all43 PNG/XML pairs (27 inherited +16 recovery), seven proof-script hashes and three adapter source hashes verified. All originals visually inspected through contact sheets and XML; recovered W05 form and deadline pair also viewed at full size.

Actual same hidden-tab read error→Back→re-entry→REST restore→Retry now opens the existing form without submission. Five stop/restore pairs target one immutable local container. Foreground stale display has no CTA; restored reads restore CTA. A server-owned deadline closes the CTA while REST is stopped, then real Back returns to discovery. Existing business/0RSD/gates assertions pass; navigation/deadline Needs acquire zero response/selection/Agreement rows.

Evidence limit: the screenshot named W04_cold_read_error contains display-only cache from the earlier navigation visit, so it proves failed re-entry, not a fresh-process uncached native failure. Component cold-read coverage remains separate. Native DB is historical79 plus explicitN02/N03, not full87 replay. APK digest is runner-reported; no independent APK binary download/byte verification. No live/provider call or new Chat/finalvisual claim.

Final PRE-P4 ran57 suites528 tests/TSC/integrity87/87/0; all3 actual CodeQL analyses have0 results/errors. Root owns acceptance/merge. Current source stays frozen; this workspace evidence does not change the APK/source.
