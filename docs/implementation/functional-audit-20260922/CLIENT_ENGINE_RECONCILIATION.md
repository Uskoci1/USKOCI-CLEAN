# Client / engine reconciliation — 2026-09-22

This is the integration department's dated audit and continuation record. It supplements the approved `REPORT.md`; execution status still belongs to `docs/control/redovi.json`. It is not a fresh live-server audit, an exhaustive security review, or a phone acceptance receipt.

## Scope and evidence boundary

- Inspected branch: `work/pre-v3-engine-integration-20260911`; committed baseline at inspection: `45a779c7357bae4ba495f335b3898849bf7fbcc2`. Reviewed the F10 changes in `d56a5726` and `45a779c7`, followed by the candidate accessibility correction described below. Root integrates subsequent parallel work.
- Read current client callers, services and selected contract bodies/candidates for discovery and own lists, application recovery, task media, safety, AI intake/profile/publication/Q&A, Inbox/push, account/export/closure, Agreement/chat/completion and reviews. References below name the actual function or component, not just an imported module.
- Server evidence is **saved evidence**: `docs/control/dev_snapshot.json`, captured `2026-09-22T13:49:06Z`, and the functional audit's `*_LIVE.json` files. The snapshot records ledger 197, 50 dev_alpha entries, last `dev_alpha_pkg045a_task_read_contract`, plus recorded worker/cron/certificate metadata. It does not establish the server's state at the time a later agent resumes.
- Later applied-package claims below come from their recorded receipts; none were re-queried here. A candidate or test file existing on disk is not proof of deployment or of a test run.
- No DEV calls, migrations, secret access, provider calls, account operations, device operations, package changes, commit or push were performed in this department pass. Only this new document is written during the reconciliation phase.
- Other agents are changing the completion review and broader control/continuity records concurrently. Their final receipt and root's final source/build verification must be read before treating those changes as integrated.

## Ranked next work

Priority indicates the order for investigation/implementation, not a measured severity score or release-completion percentage.

| Priority / existing reference | Current integration gap | Smallest next step and decisive evidence |
| --- | --- | --- |
| 1 — F16, control A05 / blocker B03 | Task-photo unknown-upload cancellation calls `rpc_cancel_media_upload`, which the saved catalog explicitly says is absent. The normal upload and avatar paths are different. | Rebase and review the existing PKG-008 cancellation proposal against freshly pinned predecessors; prove cancel-before-claim, delayed upload, already-admitted asset, ownership and closure interaction on a disposable database. Apply only through the separate server process. Preserve the local request identity until an authoritative outcome. |
| 1 — F05, B08 / N06 / N07 | Public task/profile entrances lack an admitted account target for reporting/blocking. `profileId` cannot be passed as `targetAccountId`. | Propose a context-bound safe target/allowed-action projection first; prove self, unrelated target, blocked pair, restricted account, world and content visibility cases. Then wire client entrances to existing safety commands and their recovery. |
| 1 — original 7.41 / remaining 8.18, N10 | Closure progress on another device cannot be discovered without the original device's START request key; the documented API guard also blocks ordinary getters/review for restricted accounts. | Separate owner-bound recovery contract through a permitted path, with certified-source impact and explicit approval handled as its own package. Test real restricted REST access with no local journal; do not bypass the guard or fabricate a START receipt. |
| 1 — original 7.17, PKG-045B | New explicit task readers are implemented; removing broad task column access is still conditional on compatible-client rollout and a certified change. | Use `pkg045/PKG045_TASK_COLUMN_PRIVACY.md` and its exact compatibility/phone conditions. The owner's conditional B approval is already recorded; do not ask again merely because this audit names the gate. Do not equate APK creation with verified rollout. |
| 2 — F06/F07/F08, B04/B05/B10 | Discovery fetches all bounded pages before local filtering; own tasks/applications and Home previews still consume full lists. The old own-task page omits current card fields. | Define filtered page/count semantics and field parity, then prove the smallest server projection extension before replacing a caller. Separate discovery from own-list paging; preserve exact named application recovery independently of a page. |
| 2 — F13/F14, D03/P01/P04 | Agreement incoming text has manual refresh/unpaged history; an event opens Agreement overview rather than the specific chat. Push transport is recorded as intentionally disabled. | First add a bounded incoming-update/history design compatible with the existing outbox and authority. Separately implement a typed event destination matrix and test real two-device delivery/cold start when enabled under the owner's process. |
| 2 — F12, D02/D13 | Agreement lacks source task/selected-application IDs and only has creation chronology. | Show only current authoritative states/actions; propose the smallest authorized workspace source-link projection separately. No inferred source IDs, arrival, ETA or fabricated timeline. |
| 2 — AI 11.1/11.2 / C01–C11 | Transport/recovery fixes exist, but model meaning, long history, complex dates and repeated/daily work are not fully accepted. | Keep offline boundary checks distinct from an explicitly authorized real-provider acceptance script. Work on one proven semantic/recovery gap at a time; no automatic paid retry or inferred support for repeated shifts. |
| 2 — N02/N04/N09 / public-release gates | Auth email delivery, legal/operator publication, export policy and delivery, and whole-device journeys remain separate acceptance conditions. | Obtain the actual operational/legal inputs, publish through their controlled process, then prove the real email/export/download/cancel/revoke flow. A working worker does not make an unadmitted export available. |
| 3 — F11/F15 | Candidate missing rating still has an account-age claim in the inspected baseline; the saved review RPC emits formal-address notification copy. | Client: distinguish known no reviews from unavailable data. Server: proposed copy-only review after inspecting the exact current body. Keep review eligibility/receipt mechanism unchanged. |

## 1. Discovery, own lists and Home

**Current client chain.** Marketplace routes retain their view/query/filter state and use `marketplaceItems` in `src/data/marketplaceView.ts`. It filters title, area and requirements by a Serbian-locale substring, price mode and rounded public bounds. `publicPoint` explicitly removes remote pins and rounds to two decimals; the map receives only task IDs and public points. `supabaseIzvor.otvorenePrilike` (`src/data/supabaseIzvor.ts:160`) calls `rpc_list_open_tasks_v3` with `p_limit:200` and the keyset cursor only. It walks up to 25 pages, then throws `OPPORTUNITIES_TOO_MANY_PAGES` if another page is required. It does not silently return the first 5,000 as a complete result. Public-profile/urgency hydration follows the task read; `odnosiPremaZadacima` independently reads batches of at most 100 named task relations.

**Contract evidence.** `DISCOVERY_LIVE.json`, entry `rpc_list_open_tasks_v3`, and `supabase/candidates/pkg023d_marketplace_bounded.sql` / `pkg023i_open_tasks_timezone.sql` are the sources for the saved contract. The stored whitelist is `category`, `priceMode`, `urgentOnly`, `remote`, `startsFrom`, `startsTo`; it has no substring query, arbitrary sort, total or remaining-slots filter. Ordering is published timestamp / ID descending. `asOf` is a read timestamp, not a snapshot token for later pages.

**Parity traps already established.** Stored SQL and the client differ at the bounds edges; the stored SQL uses a margin and refuses broad/wrapped/zero-area boxes accepted by the client bounds representation. Remote-only with a box is invalid. Comparisons against `starts_at` omit undated flexible schedules. `acceptsApplications` is not equivalent to filtering every unavailable task out of the response. These exact issues are explained in `DISCOVERY_SEARCH.md`, “Next contract preflight”; do not replace a full read with one page plus local search.

**Other list families are distinct.** `needClientService.mojePotrebe` (`src/data/needClientService.ts:143`) uses `rpc_list_my_tasks`, not the older page. `applicationClientService.mojePrijave` (`src/data/applicationClientService.ts:72`) uses `rpc_list_my_applications`. `rpc_list_my_needs_page` in `DISCOVERY_LIVE.json` lacks `priceBasis` and selectable-application count required by current card mapping. The page-family source is `supabase/candidates/pkg023a_own_reads_paged.sql`; cancelled-Agreement classification parity was subsequently patched by `pkg042a_cancelled_agreement_read_parity.sql`. Existing page APIs must be checked against those later changes before adoption.

`src/app/(app)/index.tsx:33` reads needs, applications, Agreements and the independent `paznjaZaPocetnu` aggregate. The latter is server-owned attention, not permission to infer missing attention from preview lists. `agreementClientService.mojiDogovori` already calls `rpc_list_my_agreements_page`; therefore “all readers lack pagination” would be false. Home's aggregate work did not eliminate the other full reads.

**Evidence/acceptance.** `DISCOVERY_SEARCH_RECEIPT.json` and `CLIENT_FOUNDATION_RECEIPT.json` record source tests, local fixture review and exact older APK evidence. The real map/filter phone observations in `DEVICE_CHECK.json` are partial and source-bound. They do not prove the new query controls with current DEV data or a scaled dataset. Next proof needs first/last page, cursor order, filter/account changes, no-results, flexible time, remote, rounded boundaries and returned-count agreement.

## 2. Offer/application seam — newly closed code concerns

The reviewed F10 slice now includes an explicit final offer review, exact total/headcount/time/message, explicit send, confirmed application ID navigation, readable cards and real candidate message preview. `src/app/(app)/prilike/[id]/prijava.tsx` uses the existing owned editor and durable `applicationCommandJournal`; successful navigation takes `data.receipt.prijavaId` and retains its current-account/focus/read and one-shot navigation fence. It does not generate a second submission.

`src/app/(app)/moje-prijave.tsx` handles the named destination; `MyApplicationsPresentation` moves a found named row to the beginning of the selected view and displays an unavailable/refresh notice for an absent named row. Only `STALE_REVIEW_REQUIRED` renders KEEP/UPDATE/changed-withdraw controls. A normal submitted offer no longer exposes those stale-task commands merely because it was named on entry. Existing filters remain authoritative.

Pending command reconciliation still calls `readApplicationCommandState` in `src/data/myApplicationsClientService.ts:31` for the exact owned response independent of the displayed list. It binds application and task IDs, version, reviewed task revision and persisted offer facts; UPDATE/KEEP are not settled by a coincidentally matching list row. This is a boundary that future paging must retain.

**Review correction in this pass.** `CandidateRow` in `src/ui/v2/ApplicationSelectionPresentation.tsx` had an explicit accessible action name, so React Native grouped the child text without announcing the new message. Its new `accessibilityHint` includes actual total, people, displayed time and at most 180 Unicode code points of the real note, and says the full message opens with the offer. The concise action label is unchanged. Rendering/reading the preview does not call the viewed mutation; opening retains that mutation and shows the full saved message. This correction modifies only that file and `src/data/__tests__/application-selection-native.test.tsx`.

**Exact verification performed by this department:** the strengthened existing preview test and added long-message test both failed on the pre-correction source, then the complete focused file passed **37/37**; TypeScript `--noEmit` and `git diff --check` passed. The earlier **242 suites / 4,710 tests** and final **96 focused tests** belong to the prior readability receipt, not to a newly run full suite after this correction. Root owns final combined verification/build.

No new owner/focus/pending regression was confirmed in the two reviewed commits. The Jest FlatList stub checks an initial rendered window, not real scroll offsets. Retained-tab re-entry, a previously selected filter excluding the named row, another named target, TalkBack/VoiceOver and exact-build native submission/landing still deserve acceptance coverage. No speculative scroll patch was made. This closes the stated F10 code gaps, not all F10 device/release gates.

## 3. Media: cancellation is the specific disconnected path

`src/app/(app)/fotografije-zadatka.tsx` persists an account/conversation-scoped upload identity, attempts the upload, and offers `cancel` for an unconfirmed result. That handler (`:120`) calls `mediaClientService.cancelUploadCommand`; `src/data/mediaClientService.ts:115` calls `rpc_cancel_media_upload(p_conversation_id,p_client_request_id)` and strictly decodes an authoritative cancellation receipt. It deliberately does not erase the intent on an absent/unconfirmed result.

`MEDIA_CANCELLATION_CATALOG.json` records `matching_functions:0` and no `(uuid,uuid)` signature on 2026-09-22. `docs/control/stanje.json` also lists this function under “app calls, server missing.” This is evidence of the recorded missing contract, not a fresh query.

The existing `supabase/candidates/pkg008_media_upload_cancellation.sql` is explicitly **source candidate only**. It proposes a private cancellation tombstone and a claim fence under the same command lock, including a delayed first upload after cancel. Its proof source is `supabase/proofs/pkg008_media_upload_cancellation_runtime_proof.sql`. Reusing that candidate requires current predecessor, closure/certificate and scope review; its presence is not deployment authorization or a passing current proof.

Do not generalize this to “media is broken.” Avatar code uses upload/read/apply/discard/clear, and Agreement photo messages have their own `agreementPhotoClientService` / `rpc_send_agreement_photo_message_v5` path. Neither is proof of task cancellation; neither is disproved by the missing cancel RPC. Their complete phone paths remain unaccepted in this audit.

## 4. Safety target and profile authority

`src/app/(app)/bezbednost.tsx` requires a non-self UUID `targetAccountId` plus validated optional task/Agreement context. `safetyClientService` binds the account incarnation, expected block revision, command request ID and authoritative receipt; it uses `rpc_get_account_block`, `rpc_set_account_block`, `rpc_submit_safety_report` and `rpc_read_my_safety_report_command`. A report lookup is a receipt, not investigation progress or an operator decision.

The public profile decoder (`src/data/publicProfileClientService.ts`) consumes `profileId`. The stored `rpc_get_public_profile` body in `AGREEMENT_PROFILE_LIVE.json` returns no account target. `PublicProfileSheet` therefore cannot safely manufacture a block/report target from its profile ID. The public task reader also intentionally exposes profile context rather than an arbitrary internal account ID. Agreement is different: its workspace includes both participant account IDs; `src/app/dogovor/[id].tsx` already navigates to safety with the actual other participant and Agreement ID.

`DISCOVERY_LIVE.json` entry `private.safety_report_context_allowed` and `SAFETY_URGENT_LIVE.json` contain the saved context/command bodies. They constrain actor/target relations and task/Agreement contexts. The smallest addition should return the admitted target/action context for the current content under those same rules. No broadened profile projection, guessed account UUID, universal report context or new moderation outcome is implied.

## 5. AI: connected engine, bounded recovery, incomplete semantic acceptance

| Flow | Actual source chain / contract |
| --- | --- |
| New/edit task conversation | `src/app/(app)/nova.tsx` → `aiTurnIntentJournal` / `aiNeedV2Production.ts` → owned open/read/recover/cancel v2 RPCs and `uskoci-ai-interview`. `submitTurn` persists identity before sending; `recoverTurn` resolves the same request. `send` bounds the native wait to 55 seconds. |
| Worker profile conversation | `src/app/(app)/profil/razgovor.tsx` → `workerAiTurnIntentJournal` / `workerAiClientService` → `rpc_open_worker_ai`, read/patch/prepare/save, turn recovery/cancel and `uskoci-worker-interview`. A saved profile requires its displayed review digest and explicit save; prose does not activate it. |
| Task review/publication | `src/app/(app)/pregled-zadatka.tsx` → `aiTaskReviewClientService` → prepare/read/latest/accept review, `uskoci-publication-evaluate`, `rpc_publish_accepted_ai_task_review`. `resumeFor` reads the stored command before evaluating/publishing; a confirmed accepted review remains authoritative. |
| Preselection question/answer | `qaSubmissionClientService` → `uskoci-qa-classify`, `rpc_read_qa_classification`, `rpc_cancel_qa_classification`. Read/cancel do not call a provider; an uncertain submit keeps its identity. |

Current source includes provider 30-second limits for task/profile interviews and independent owned failure settlement (`supabase/functions/uskoci-ai-interview/index.ts`, `uskoci-worker-interview/index.ts`, `uskoci-qa-classify/index.ts`). SQL counterparts are `pkg039a_intake_failure_settlement.sql`, `pkg040a_qa_failure_settlement.sql`, and the existing recovery/sweep authority; publication uses `pkg037a_publication_review_expiry.sql`. A client timeout cannot prove the server stopped. A lost claim response/process death/failed cleanup still needs the durable recovery/sweep path; do not introduce automatic paid replay.

Recorded deployment evidence: PKG-037/039/040 receipts, PKG-043 transport repair, and PKG-044's `TONE_EDGE_RECEIPT_20260922.json`. The saved control snapshot identifies intake 50, worker 17, Q&A 13 and publication 14. This department did not verify hosted bytes or call a model. Owner-reported successful/warm task conversation is limited evidence; it is not a worker-profile or universal-quality pass.

`v5-ai-first/ai-conversation-audit-20260922/REPAIR_COVERAGE_20260922.md` remains the precise semantic map: correct action classification and prose, complex/negated/quoted dates, long history (provider context uses last 30 messages), and daily/repeated work remain limits. Existing finish handling leads to explicit review; no generated statement is a success receipt. Follow that map rather than re-opening already fixed transport defects or claiming model accuracy from mocked outputs.

## 6. Inbox, settings and push are separate contracts

`inboxClientService.list` already uses a cursor and `hasMore` from `rpc_list_inbox`; `resolve` validates a closed union of `AGREEMENT`, `APPLICATIONS`, `CANDIDATES`, `OWN_NEED`, `OPPORTUNITY`, or `UNAVAILABLE`. `src/app/obavestenja.tsx` routes from that result, including named application ID and a question-specific destination for `CLARIFICATION_*`. It does not use free-form notification body text as navigation authority.

The saved/current-source resolver contract is also represented in `supabase/candidates/pkg045a_task_read_contract.sql`, `rpc_resolve_activity_event`. A response event may resolve to an existing Agreement, otherwise to a worker application or owner candidate list; current task ownership uses `is_my_task`. This is deliberately coarser than a chat tab, an exact candidate detail, or a particular message. F14 remains a typed destination/emitter/recipient contract task, not “Inbox missing.”

F01 and F03 are fixed in source: `PushPreferences` keeps writes under `busy || error`, but the error recovery action is disabled only by `busy`; notification timestamp font is 12. Keep the first behavior when polishing the screen. The status index's older statement that retry is still disabled is historical.

Push device registration/preferences are distinct from delivery. `supabase/functions/uskoci-push-transport/index.ts:72` reads the enable switch; the payload at `:120` is generic and has `data:{kind:'INBOX'}`. The saved snapshot/PKG-030 records operational scheduled workers, while control P04 records transport intentionally off. No exact content preview, message destination, revoked-token delivery, quiet-hour result or two-device receipt is proven here. P05 reminders remain a proposal: inspect existing events/scheduler machinery before inventing another server subsystem.

## 7. Account, export and closure

**Auth.** `auth.tsx` reads admitted auth methods before enabling email flows; `authClientService` uses Supabase sign-in/sign-up and the configured recovery redirect. `passwordRecoveryClientService.createRecoverySession` binds the callback to the current account revision and validates the recovered identity. The signup screen now honestly describes unpublished test-version legal documents rather than recording a meaningless consent checkbox. These source paths do not prove email arrival, actual recovery links, legal publication, A→B→A or iOS acceptance. Control N02 still identifies email delivery as unproved.

**Export.** `src/app/(app)/profil/izvoz.tsx` → `dataExportClientService` → request/status/cancel/revoke RPCs plus `dataExportDeliveryService` / export Edge functions. The client checks receipt ID, request identity, lifecycle, artifact generation, expiry, byte size and digests; file saving has its own account/focus/abort fence and clears downloaded bytes. A READY label alone does not admit a download. `pkg029d_export_honest.sql` makes a missing policy reject new requests with `DATA_EXPORT_POLICY_NOT_READY`; the client maps that refusal explicitly. Saved PKG-030 worker activity is not proof that a new export is policy-admitted or delivered. Legal retention input, actual delivery/save/revocation and interruption evidence remain distinct next steps.

**Closure.** `ClosureDialog.restore` in `src/ui/closure/ClosureDialog.tsx` loads `closureIntentJournal`; a START intent calls `closureExecutionClientService.read(clientRequestId)`, otherwise it calls execution review. Preparation uses `accountClosureClientService`; its legacy contract intentionally decodes `restricted:false` and `executionReady/authClosureReady/mediaCleanupReady:false`. Execution uses a separate strict review/start/read contract and a second explicit irreversible confirmation. Do not “fix” legacy false flags to true or merge the two decoders based on a successful worker receipt.

The remaining cross-device gap is documented in `NEXT_AI_HANDOFF_20260921_CODEX.md:119`: without the local START key, the current read cannot discover execution, and the recorded `rpc_closure_api_guard` blocks the ordinary getter/review once restricted. The guard and execution reader belong to the certified surface. Preparation parity was corrected separately by `pkg034a_closure_preparation_blockers.sql`; that does not solve cross-device recovery.

There is real **historical disposable** closure proof: `pkg032/PKG032_CANCEL_REASON_AND_GUARD.md` records run `35643688275`, real Auth signup and exact worker closure across 75 steps, including removal of a cancellation reason; later PKG-045 also contains a disposable closure proof for its candidate. Those are stronger than source existence and narrower than accepting the owner account on current DEV/phone. No owner account was closed here. General retention/legal readiness, erasure certificate readiness and the legacy preparation flags must not be collapsed into one green status.

## 8. Agreement, messages, completion and ratings

`agreementClientService.mapAgreement` maps accepted terms and participant accounts/profile IDs. Its `agreementActions` uses the exact workspace `actionState` bound to Agreement ID, version and current account; missing/foreign/malformed action state yields no completion authority. `decodeCompletionReceipt` admits only the exact terminal COMPLETED receipt. `acceptedSchedule` renders accepted instants under the recorded Serbian-time rule; parent task edits do not rewrite accepted terms.

`AGREEMENT_PROFILE_LIVE.json` entry `rpc_get_agreement_workspace` confirms the saved projection has participant IDs but no source task/selected response IDs. The current mapper's chronology is a single creation event. D02/D13 therefore cannot be completed by inventing links or a moving/arrived timeline. The concurrent agent's `AGREEMENT_COMPLETION_REVIEW.md` must supply the source/token/focus/read-invalidation evidence for its new confirmation UI; this audit does not certify unfinished work or change the completion mutation.

**Chat.** `src/app/dogovor/[id].tsx` reads text through `useFocusedResource` and `supabaseIzvor.poruke`; the latter selects `agreement_messages` ascending by created time and ID with no page bound. `src/ui/AgreementChat.tsx` renders all text in a ScrollView with manual refresh and follows local outgoing content. `agreementMessageClientService` sends through `rpc_send_agreement_message_v2` (or the separate photo RPC) using the existing immutable outbox identity. Keep account/Agreement scope, ACK reconciliation and pending commands while improving incoming freshness or history. Do not infer that group chat or photo history shares precisely the same pagination/recovery behavior.

**Reviews.** `oceni-dogovor.tsx` / `AgreementReviewScreen` → `reviewsClientService.context/submit` → `rpc_get_my_agreement_review` / `rpc_submit_agreement_review`. The context supplies the other account, eligibility and exact tag catalog; submission validates rating 1–5, at most three tags and the immutable command/receipt. `rpc_get_account_reputation` provides a known NO_REVIEWS/RATED aggregate. A public “new user” claim is not a substitute for an unavailable aggregate. The actual review source is `supabase/migrations/20260912100000_clean_pre_v3_reviews_authority.sql`, with saved later bodies in `REVIEWS_LIVE.json`; the formal-address copy finding F15 belongs to that saved submit body. Existing completion/review mechanics should be retained while changing presentation/copy.

## Continuity and acceptance rules for the integrator

At inspection, generated `docs/control/stanje.json` metadata still named `d56a5726`, while committed code was `45a779c7`; `USKOCI_CURRENT_STATUS.md` also contained older installed-APK and retry-defect statements. This is a dated reconciliation observation. Root should refresh the control projection/status pointers after integrating current work; do not treat an old generated heading as newer source evidence or maintain this document as another live control table.

For the next package, attach four separate conclusions: **source chain connected**, **specified automated proof passed**, **exact server revision applied/read back**, and **exact build/phone flow accepted**. Mark each only from its own evidence. The last full-suite number, catalog snapshot or APK checksum cannot substitute for the others.

Minimum acceptance still required for the affected flows: both account sides on the same exact build; named-offer landing and missing target; stale offer review/retry; incoming chat and its destination; task media interrupted cancellation; safety target/context; publication versus AI prose; export policy/refusal/download; restricted closure with/without the local journal; completion/problem/pending-change/review; background, network interruption and A→B→A. These are acceptance gaps, not claims that every listed source path is defective. No destructive account operation or paid/provider/device test is implied by this audit document.
