# UX execution reconciliation — 2026-09-22

Later root checkpoint: the pending client changes below are committed as9286fdeb. Combined types
and242 suites /4719 tests passed with natural exit0 (known teardown warning). Final APK35775425984
is tracked in PRODUCT_EXECUTION_RECEIPT. The preceding45a APK was installed and My Applications
visually checked; earlier50178 offer review/Android Back preserved an unsent draft. This adds narrow
native evidence, not a full submission/completion acceptance. Root has also corrected the main
plan/status/entry map; older drift observations below retain their inspection date.

This is a bounded source reconciliation for the unified finishing plan, not a second execution tracker. The living row state remains `docs/control/redovi.json`, generated according to `docs/control/README.md`. The approved analytical baseline is `REPORT.md` and its 48-section matrix. Reconcile facts below into those rows; do not calculate completion from route, service, RPC or test-file counts.

**Observed source anchor:** branch `work/pre-v3-engine-integration-20260911`, HEAD `45a779c7` after `d56a5726` and the offer-review slice. Read on 2026-09-22 in the canonical audit worktree. Candidate accessibility and Agreement completion work were concurrently in progress. Their uncommitted bodies are observations, not an accepted release or an independent review. This audit changed only this document, ran no application tests, called no DEV/provider/device, and created no commit.

## Binding interpretation and conflicts to carry forward

- `OWNER_DESIGN_DIRECTION_20260922.md` and `UX_NACRT_20260922.md` govern each screen. The owner approved the functional analysis and D1–D10. The earlier analysis waiting gate is historical. Preserve Inter, measured colors, colored FactArt, TaskCard and the inset navigation character; V28 supplies inspiration, not mandatory geometry. Green titles are approved. There is one account, with two activities, not a global role switch.
- D1–D10 remain necessary: missing mandatory facts cannot be confirmed away; unknown outcomes are not success; offline business writes are not automatically queued; viewed/shortlisted are optional; closed search is not completed work; a safe report target is not a profile UUID; urgent policy is not enabled by RPC existence; public sharing requires a safe destination; paging must preserve price/visibility/count/search semantics; long focused editors may omit the tab bar while retaining Back.
- `APP_FINISHING_PLAN_20260922.md` remains the R1–R9 release framework, but its dated facts need reconciliation. Its “solo / no delegation” statement is superseded by the owner's current explicit request for maximum independent agents. Its R5 “no final app-wide direction” and initial R2 settings-retry instruction predate the approved design direction and F01–F04 source fixes. Its measured APK/test counts and `USKOCI_CURRENT_STATUS.md`'s “latest” anchors predate later functional slices. Keep package dates/source receipts rather than treating these lines as current blockers.
- The newest owner instruction permits client progress and actual-component review while sign-in is unavailable. This does not waive the per-screen exact APK, authenticated phone and independent Claude acceptance. A browser fixture can establish composition behavior; it cannot establish native geometry, server outcomes, maps, push, permissions or acceptance.
- No new backend, guard, recovery, credential, dependency, charging or policy change follows from UX approval. Only Gorhom 5.2.14 has the recorded package approval. PKG-045b and its certificate rebind are already owner-approved **conditional on verified compatible app rollout**; this audit neither satisfies that condition nor asks for the approval again.
- Additional draft mismatch found in current bodies: §5 step 4 broadly says “edit offer.” The current waiting-offer UI supports withdrawal; editing is exposed only for `STALE_REVIEW_REQUIRED`. `moje-prijave.tsx` explicitly rejects non-stale actions other than withdrawal, and its update calls the changed-application resolver. A proposed time remains unchanged in that editor. Before offering ordinary submitted/viewed/shortlisted edits or time changes, verify the intended product and server contract. Do not broaden RU4 by presentation alone.

## Evidence that has actually advanced

| Slice | Source/proof recorded | Build and native limit |
| --- | --- | --- |
| F01–F04 client foundation | `CLIENT_FOUNDATION_RECEIPT.json`: typecheck exit 0; 242 suites / 4,698 tests; source `8f0be1f`. Current bodies confirm retry remains available on error, notification timestamp 12, and navigation-history-based primary highlighting. | ARM64 artifact verified; x86_64 build installed and welcome/sign-in observed. Authenticated screens and physical-phone acceptance pending. An older ARM-only APK's emulator launch failure is historical, not hidden. |
| Discovery search | `DISCOVERY_SEARCH_RECEIPT.json` and `DISCOVERY_SEARCH.md`: 242 / 4,700 and types; actual component checked at 320/390px for search, price apply/remove and basic states. | Both `6d3c7ead` APK artifacts verified; emulator startup only. F06/F07 server filter/paging work is still open. |
| Offer review | `OFFER_REVIEW_RECEIPT.json`: explicit final review, same route validation/journal/recovery; 242 / 4,704 and types. | Both `50178e6b` APK artifacts verified, emulator welcome/auth only. This does not cover the later landing/readability commits. |
| Named offer receipt landing | `OFFER_LANDING_RECEIPT.json`, committed `d56a5726`: receipt `prijavaId` routes to that owned application; missing named row is explicitly unavailable; stale editor controls remain scoped. 242 / 4,709, types; three regressions failed before. | Exact new build, authenticated flow and Claude acceptance pending. |
| Readability and candidate message preview | `OFFER_READABILITY_RECEIPT.json`, committed `45a779c7`: total price and people wrap, own message remains visible, real candidate note preview is present. 242 / 4,710; final types and 96 focused checks; browser fixture only. | Exact new build and independent review pending. Candidate accessibility and Agreement completion were being changed concurrently; do not transfer these earlier test results to those edits. |
| Candidate rating wording F11 | During this reconciliation, the parent checked the candidate projection and changed all three missing-rating fallbacks to “Ocena nije dostupna.” A final source read confirmed those three replacements. | Dirty source at this handoff; combined checks/build/native acceptance pending. This is a narrow copy fix, not complete profile/reputation acceptance. |

The earlier phone receipt for source `31598ac8` showed signed-in Home, map rendering and filter opening/price draft count, and exposed the narrow legend defect. It did not accept the entire sheet, detail or journey. Historical PKG-042 Home launch and PKG-043/044 owner-observed AI transport/replies likewise cover only their stated scope. No current-build full two-person journey was established here; `test_dva_telefona_izvrseno` was empty at inspection.

## Journey-to-execution map

“Code” below means a body or route behavior was inspected, not executed. “Recorded” identifies prior audit/package evidence not re-proved in this pass. The native column is required evidence, not an instruction to bypass unavailable owner sign-in.

| Journey / control rows | Implemented capability to retain | Remaining UI, client or contract gap | Native acceptance needed |
| --- | --- | --- | --- |
| Entry, auth and recovery — N01–N03, S01–S02 | Auth route has availability-gated methods, email sign-in/sign-up and recovery forms; recovery route has verification/retry/new-link states. Current shell has Home / Map / Agreements and no global role key. | Email confirmation delivery remains unproved in control. Full legal consent, deep-link/login return and available-provider configuration need end-to-end verification; do not infer from controls. S01 still describes the already-corrected lost-highlight source defect. | New account, actual email/recovery link, cancelled/expired link, cold restore, intended destination and A→B→A isolation on the current build. |
| Home / activities — A01 | Home explicitly reads the owned attention aggregate plus task/application/Agreement previews, tolerates partial unavailable sections, and routes by typed subject. Two primary actions lead to intake and discovery. | Previews still call full readers. Attention integration does not establish paginated Home or reduced fetch cost. Preserve the four approved reasons and distinguish unavailable from zero. Activities body was not re-read in this pass. | Account holding both activities; each attention action; empty and partial read failure; nested Back/tab indication and returning to the correct item. |
| Worker preparation and schedule — B00–B03, B12 | Profile hub exposes skills/team, area, recurring availability/exceptions and schedule. Worker editor retains attempted writes and confirms readback; availability warns when the worker profile is only a draft. Calendar links actual Agreement IDs. AI interview/activation exists in recorded audit. | Active profile must not imply push delivery. Shared identity/avatar/contact across two role profiles needs explicit authority. Worker AI quality, long history and device voice remain unaccepted; do not rebuild availability as absent. | Prepare and activate B; edit capacity/area/availability; timezone/DST and overlap; selected Agreement appears in calendar; interruption and profile draft/suspended states. |
| Discover — B04–B05 | Persistent search, list/map switch, selected pin sheet, price filter remove/apply/cancel and count from the displayed local result are present. The route refreshes on focus and checks current account/context before navigating. | F06/F07: reader walks up to 25×200 rows, then filters locally; excess throws. Existing SQL lacks text/sort/total/remaining-slot semantics and differs on flexible time, bbox edges/wrap and remote tasks. Do not page first and then search one page. See `DISCOVERY_SEARCH.md` contract preflight. | Real map and selected-pin→detail→Back; actual count/apply/reset/dismiss/hardware Back; keyboard at 361 logical px and increased font; another phone filling/cancelling a task; slow/failed read. |
| Decide on a task — B06–B08 | Public detail shows authoritative facts/price basis/requirements/description/photos/approximate location/Q&A/publisher. Footer distinguishes owner, applied, unknown relation and eligible new application. Public profile sheet honestly distinguishes known zero reviews from unavailable rating. | F05 contextual report/block target is missing; share destination is not established. A public profile UUID must never be passed as account UUID. Layout and sticky footer are source implemented, not phone accepted. Public individual review history is not implied by aggregate. | Detail with/without photos, no price, per-person total, remote task, closed task, own/applied relation, long text, public-profile open/close, and large font with reachable footer. |
| Compose / review / send offer — B09 | Review shows total, people, actual exact or flexible time and note; final send uses existing route validation, durable request identity and explicit uncertain-state readback. Confirmed receipt opens exactly its application. | F10 is source advanced, not complete. Account/refresh/task-change invalidation and receipt are essential. No success from dismissal or spinner; no generic list destination instead of the confirmed row. | Both price modes, TOTAL/PER_PERSON, multi-person arithmetic, time proposal, missing data/refusal, cancel review, duplicate tap, lost ACK, remount and confirmed row landing. |
| Track / change / withdraw offer — B10–B11 | Status tabs, focus on named application, missing-row notice, existing task/Agreement links, note/total/people and withdrawal confirmation exist. Changed-task review can keep/update/withdraw using versioned command. | Full list read remains; paged substitution must preserve named pending reconciliation. Ordinary waiting-offer editing and time editing are not implemented as the broad draft wording suggests; see conflict above. | Named row outside initial viewport, filtered list, missing/inaccessible row, task revision after send, stale offer review, withdrawal, interruption and multiple account sessions. |
| Create task with AI / voice — A02–A03 | Intake creates a conversation on first send or explicit speech preparation, not merely viewing. Account-scoped durable turn recovery, preserved last data, draft summary, missing-fact hint, inline location prompt and explicit review exist. Dictation appends editable text before explicit send. | Recorded deployed AI fixes improve transport/recovery; model quality, complex dates, repeated work/pricing meaning and history limits remain. Do not promise a complete spoken assistant or automatically reissue a paid attempt. | Owner-initiated task dialogue from zero, corrections/finish, location transition, timeout/readback, app background, dictation permission/cancel/transcript edit. Android/iOS support separately evidenced. |
| Location / task photos — A04–A05 | Location is explicit, private points remain private and public map stays approximate. Photos have selection, camera/gallery, receipt readback, same-command retry and removal. | F16 recorded missing `rpc_cancel_media_upload` is still called from task-photo cancel; uncertainty is retained. Review/prove PKG-008 before a separate server apply. Avatar does not use this cancellation route. | Denied/regranted camera/gallery/location, cancelled picker, upload failure/lost ACK, late upload versus cancellation, photo limits, private/public projection and remote task without a pin. |
| Review / publish — A06–A07 | Review has public/private projections, fact correction, location/deadline editing, missing-data/unsupported-identity blocking, immutable command/resume and exact published readback before “open task.” | D1 overrides the draft's loose “confirm when something missing.” Required facts and current server readiness remain blockers. Broad AI/server failure recovery is recorded; real native publication still needs proof. | Public view matches reviewed terms, exact points hidden, TOTAL/PER_PERSON, save draft versus publish, moderation/refusal state, interrupt and reopen same command, confirmed destination. |
| Own task, edit/cancel/close search/urgent — A08, A13–A16 | Owned route presents lifecycle actions and Q&A, opens review/edit conversation, confirms remaining-search closure with preserved command, routes to candidates. | Task cancelled, search closed, filled and work completed must stay distinct. F09: urgent API exists but recorded policy is disabled with no allowed categories. Reposting is a proposal, not present by implication. | Draft deletion, edit with/without Agreement eligibility, cancellation reason, partially filled search closure and unchanged existing Agreements; stale confirmation and return navigation. |
| Own task list — A09 | Owned task/card presentation and selectable-versus-historical application distinction already exist in recorded PKG-035. | F08 paged reader lacks price basis and selectable count parity. Contract must be extended/proved before replacing full reads. | Draft/published/partial/filled/closed/cancelled rows; correct action count; page boundary and newly changed task without duplicate or missing row. |
| Public questions / answers — A10, B07 | Task-specific route and Q&A entry are wired for owner and public contexts; clarification inbox events already route to that task's Q&A, not generic detail. Existing recovery package is recorded. | Audit current-task revision, visibility, moderation/answer limits and actual delivery before claiming complete. A full screen instead of a short panel is within approved D8 when content requires it. | B asks, A answers, both see correct revision/public answer; denied/stale/refused/unknown response and notification destination. |
| Candidate review / comparison / selection — A11–A12 | List has real note snippet, identity/photo, total, capacity, time and state. Full offer and adaptive comparison exist. Selection already confirms actual terms and reads an existing Agreement on recovery. | Initial inspection reproduced F11's unsupported “new” label; the parent fixed all three fallbacks to “Ocena nije dostupna” during this pass and the final source read confirmed them. The wording fix and concurrent accessibility patch still require combined checks/build/native evidence. | Candidate list→single offer→profile→comparison→selection; full note versus snippet; width/font fallback; two simultaneous selections, overfill/stale refusal; real Agreement receipt and both participants. |
| Agreement list / overview — D01–D02, D13 | List is a separate destination; workspace shows accepted terms, actual status/next action, overview/messages, participants and private sections. `AgreementHero` uses accepted Agreement facts rather than current task. | F12 workspace lacks source task/selected application IDs; creation-only chronology is not progress evidence. Truthful state/next-action presentation can improve now, but source links need an authorized projection. No invented arrival/ETA or “on my way.” | Both roles and terminal states; correct accepted terms after task edit; pending-change/problem blockers; header, footer, Back and review/action priority with large text. |
| Messages / attachments / group — D03–D05 | Protected outbox, explicit unknown/failed/confirmed states, sender/key/body/photo reconciliation, private attachments, support context and outgoing scroll-follow exist. Group entry is contextual. | F13 bilateral incoming messages still require refresh; reader is unpaged ascending and UI maps all messages in ScrollView. Pagination/freshness must retain authorization, cursor scope, outbox and read-only boundaries. Old callable send version is a separate backend exposure. Group engine was not re-audited here. | Two devices send/receive without manual refresh after the change; history pagination while reading old messages, keyboard, attachments, denied/read-only access, offline retry and no duplicate bubble; multi-person group isolation. |
| Change, cancel, location/contact, problem — D06–D09 | Existing routes/controllers and workspace entries support term changes, private location/contact, context safety and problem reporting; problem command distinguishes uncertainty. Recorded changes compare old/new and require other-side acceptance. | Keep single captured location separate from live tracking. Report receipt is not operator resolution; support case cannot silently change Agreement, price or rating. Contact cancellation/sharing and pending changes need full two-side proof. | Proposal accept/reject/withdraw/stale version; cancellation with reason; grant/revoke location/contact and permission denial; problem blocks automatic completion while permitted explicit requester confirmation remains, another device sees state; support escalation. |
| Complete and review — D10–D12 | Server action state controls eligible completion. Existing review screen has eligibility, stars/tags and receipt. Completion confirmation component/route was being implemented concurrently during this read. | Re-read final completion patch and its dedicated evidence before updating rows. Required copy: who confirms, authoritative deadline and conditional auto-completion; do not promise automatic completion with an open problem; explicit requester confirmation follows actionState and pending-change guards. F15 formal server review notification copy remains recorded. | B marks done→A confirmation→both see completion→both ratings; cancel confirmation, blocked/pending change, deadline/auto path and duplicate/lost ACK. Independent review plus exact build required. |
| Inbox / preferences / push / reminders — P01–P05 | Inbox is actually paginated with unread count, filters, stale-content notice and owned resolver. F01 retry no longer disables itself; writes remain locked on uncertain state. Q&A receives specific navigation. | F14 Agreement targets do not select chat for `MESSAGE_RECEIVED`; resolver has five coarse target kinds. Recorded push remains transport-disabled. Prove all 24 emitter/recipient/preferences/privacy/target paths; do not promise delivery from activation. Reminder is proposed, existing scheduler/event support must first be checked. | Permission deny/regrant, settings error recovery, quiet hours, foreground/background/cold start, other account/deleted subject, actual provider→OS→tap trace on two devices and eventually iOS. |
| Identity/avatar/settings — N05 | Grouped profile hub reads identity and worker capability; real avatar opens the dedicated photo flow, reputation has its own reader, logout is scope guarded. | Role profile rows remain distinct; no silent mirroring. Missing task-photo cancel RPC is not proof avatar flow is broken. Optional phone remains a product/authority question. | Real avatar set/change/remove, profile edit/readback, all settings routes/Back/selected tab, logout and A→B→A without stale personal data. |
| Safety / support — N06–N08 | Safety route requires account UUID and optional owned task/Agreement context. Agreement supplies real participant account ID. Support inbox/authoring/case/operator surfaces already exist in recorded audit. | Task/public-profile entry missing safe target context (F05); adding button alone is unsafe. Operational moderation contact, permissions, queue ownership and outcome need evidence. Receipt is not investigation progress or resolution. | Report/block from permitted contexts, blocked-pair behavior and unrelated third account; user→operator→response/escalation; inaccessible evidence stays private. |
| Legal, retention, export, closure — N04, N09–N11 | Legal controller reads bundle/processor map and records reviewed acceptance. Privacy has policy/version/execution readiness and closure entry. Export separates status, preparation, downloadable availability/expiry and actual file save. Recorded disposable closure proof exists. | Published legal/operator inputs and retention decisions remain gates. Recorded restricted-account/legacy closure readiness and second-device discovery of execution are unresolved; no deletion attempt on DEV follows from this review. Public deletion-request page remains release work. | Consent correct version and external docs; real export request/file/expiry/revocation; closure progress/recovery/retained data on designated disposable test accounts; second-device and account-state transitions. |
| Reliability / delivery — S03–S04 and store gates | Many current routes explicitly fence focus, account revision, stale reads and command identity. Compatibility routes serve old entry points. | Structural lights do not prove all families preserve those fences. Old authenticated RPC exposure, remaining 7.1 call families, production/load/backup/release configuration and store gates are separate work. Connecting-service charge before public release is required but model is undecided. | Test interruptions at actual command boundaries, A→B→A, third account isolation and current-source APK attestation. Signed Android/iOS, privacy forms, support operation and approved charging flow need their own evidence. |

## Ranked implementation and verification sequence

1. **Close the present client slice coherently.** Finish and independently review candidate accessibility and Agreement completion review; include the parent's narrow F11 copy fix in meaningful focused regressions/types and the final suite for the integrated source. Record exact source hashes. Update the stale A11/S01/F10 descriptions, keeping phone/Claude evidence pending. Do not revert to old “missing review/snippet” statements.
2. **Build one exact current source and accept the already-implemented discovery→detail→offer→named application→candidate selection spine.** Use authorized owner sign-in and real participants when available; retain lower evidence levels until then. Layout checks include small width, large font, reduced motion, TalkBack/VoiceOver order, keyboard and Back. This is also the opportunity to verify PKG-045 compatible task reads, but R1 rollout conditions remain a separate recorded decision.
3. **Prepare narrowly scoped server contracts alongside permitted client work.** Highest user impact: F05 safe context target and F16 media cancellation; discovery F06/F07 with predicate/count/page parity; F08 own-list projection; ordinary offer-edit semantics; F12 Agreement source links. Each requires current live body preflight, candidate/disposable proof and the applicable approval before apply. Do not use the saved audit captures as a fresh DEV certification.
4. **Finish path A and Agreement under the accepted UX, preserving the engine.** Review intake/task review/location/photos, own task, candidate detail/selection, truthful stage/next action, changes/problem and completion/review. Implement F13 fresh/paged incoming chat with account/visibility/outbox parity and prove cross-device behavior. A screen may be visually polished while its contract or phone gate stays open.
5. **Unify attention and notification destinations, then actual controlled delivery.** Define all 24 events by source, recipient, preferences, privacy, owned destination and cold-start behavior. Prefer action-specific landings using validated event metadata. Keep message content off lock screens unless an explicit policy approves it. Home preview pagination is separate from the already-integrated attention aggregate.
6. **Finish account/safety/support and release operation in parallel with device opportunities.** Worker onboarding, profile, availability, legal/export/closure, actual operator response, public deletion page and charging decision belong in R3/R6/R7. Finish R8/R9 production, monitoring, distribution, iOS and store evidence before calling the app releasable. Saved alerts/repost/reminders/full spoken AI/live arrival are proposals until selected and contracted.

## Durable continuation protocol

At the next session, first confirm the canonical worktree/branch/HEAD and inspect uncommitted work. Read owner direction, approved D1–D10, current control rows and newest package receipts. Identify each agent's file ownership before writing. Do not derive current source status solely from the top of `AGENTS.md` or an old “latest APK” sentence.

For each screen record: its one user decision; draft agreement/gap/deviation; existing command/read ownership; selected composition; loading/empty/error/stale/unknown/success; focused test result with source; build/hash; exact native path and account roles observed; independent Claude result; residual contract/policy/release blocker. If native input is unavailable, continue permitted independent work and label acceptance pending. No auth bypass, copied session, fixture DEV row or implied test account is a substitute.

After integrating a slice, the parent maintains `redovi.json`, regenerates `stanje.json`, and publishes via the existing owner artifact flow. This reconciliation agent deliberately did not modify those shared files. Generation is not publication, and a prior upload does not prove the new snapshot is visible.

## Inspection boundary and file record

Policy/status read: top/current entries of `AGENTS.md`; complete owner direction and UX draft; functional `REPORT.md`; control README and all 62 compact row facts plus blockers/store/two-phone plan; current-status and finishing-plan opening/queues; foundation/discovery/offer/landing/readability receipts; `DISCOVERY_SEARCH.md`. Existing broad-audit and server claims above are attributed to these records, not independently re-certified.

Bodies inspected, in full or the relevant indicated paths:

- Navigation/Home: `src/app/(app)/_layout.tsx` (full), `(app)/index.tsx` (full), `src/ui/home/HomePresentation.tsx` (presentation sections), `(app)/prilike.tsx` (read/navigation), `(app)/mapa.tsx` (wrapper).
- Discovery/detail/offers: `src/data/supabaseIzvor.ts` (`otvorenePrilike`, `prilika`, task relations, `poruke`); `src/ui/v2/PublicNeedPresentation.tsx` (full); `(app)/prilike/[id].tsx` (scope/reads/actions); `(app)/prilike/[id]/prijava.tsx` (read/journal/submit/receipt); `(app)/moje-prijave.tsx` (read identity/intent/edit/navigation); `MyApplicationsPresentation.tsx` (full body); `(app)/potrebe/[id]/kandidati.tsx` (read/select/receipt); `ApplicationSelectionPresentation.tsx` (composer, review, identity, rows and compare).
- Path A: `(app)/nova.tsx` (full route); `IntakePresentation.tsx` (summary/recovery/location composition); `(app)/pregled-zadatka.tsx` (read/accept/block/result and rendering/action paths); `(app)/fotografije-zadatka.tsx` (journal/recovery/upload/cancel/render actions); `(app)/potrebe/[id]/pregled.tsx` (lifecycle confirmation/edit/navigation).
- Agreement/notifications: `src/ui/v2/AgreementPresentation.tsx` (full); `src/app/dogovor/[id].tsx` (workspace/action guards/completion/sections and navigation, including concurrent work); `src/ui/AgreementChat.tsx` (full); `src/app/obavestenja.tsx` (resolver/render); `src/data/inboxClientService.ts` (full); `src/ui/notifications/PushPreferences.tsx` (retry/edit/save/render locks).
- Profile/account: `(app)/profil.tsx` (full); `profil/radnik.tsx` (read/attempt/readback); `profil/dostupnost.tsx` (full); `profil/privatnost.tsx` (policy/execution/closure presentation); `profil/izvoz.tsx` (status/preparation/download availability); `profil/pravna.tsx` (controller/action paths); `src/ui/system/PublicProfileSheet.tsx` (full); `(app)/bezbednost.tsx` and `podrska/index.tsx` (full route wrappers); auth/recovery/calendar/review entry/action searches.

This pass did **not** semantically read every controller, server function, guard, test or screen. Full support/operator/group/location/closure/worker-AI internals, all deep-link/auth branches, accessibility behavior, full style rules, every notification emitter, current live DEV state and actual phone flows remain outside its independent evidence. Follow the earlier 48-route matrix for coverage inventory and inspect the current body before editing any such surface. No exhaustive-whole-repository or all-flows-pass claim is made.
