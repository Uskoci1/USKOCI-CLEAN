# V5 canonical DEV/ALPHA promotion — authorized preparation

Owner AF-D26 explicitly authorizes verified migrations, RPCs, Edge Functions,
Storage and configuration on `leqcwgzvjsxugfgzdmth`. No additional staging or
blanket live approval is required for this project. A separate future production
project is outside scope. This document records preparation, not execution.

The latest prepared JSON before photograph144 integration binds saved source
`78bbd1cb338ea0d51e4924f16bf8f1a34a241c8d`, tree
`2ac97f7b45d96f2bc187429ca1d69d2e23c6fce7`,35 migrations109–143 and11 Edge
functions. The registered144 candidate adds private Agreement photographs;
regenerate JSON/complete Edge payloads from the saved source containing144
before any promotion. The generator reads Git objects, verifies raw migration
MD5/SHA/bytes against provenance, and records full per-function dependency
payloads. Uncommitted files are never a deployment input.

Latest exact-source FULL143 run34744679836 on78 is FAIL:11 source gates,
190 Jest suites/3949 tests and740 Node tests passed, then135 passed five checks
before LOCAL_RPC57014.136–143 were not reached. Earlier source70 actually
passed through137, and earlier136 executed real export handlers/Storage.
Those scoped results do not replace required FULL144/34 actual reports.
The135 proof now gains bounded labels/timings, not longer timeouts or retries.

Fresh canonical preflight at07:14:22UTC still observes108 migrations,3 Auth
accounts,7 Tasks,2 Agreements/versions,0 profile-media objects and0 retention
policies; two private buckets, unchanged existing cron and five Edge versions.
All108 predecessor statements match historical87 or exact source/alias21
hash evidence. See DEV_ALPHA_PREFLIGHT_20260913.json; refresh immediately
before promotion, and stop on unexplained drift.

The authorized sequence is:

1. Save/review source, complete disposable144 proof, verify predecessor hashes,
   existing business/evidence rows, configuration and backfill cardinalities.
2. Apply the exact forward files in order, one transaction per file; record actual
   versions/aliases and executed statement hashes. Preserve historical migrations,
   accounts, existing Tasks and Agreements. No reset or history repair.
3. Deploy all11 reviewed Edge payloads with explicit JWT verification and verify
   actual bundle/asset dependencies, Auth and hosted HTTP/WebSocket behavior.
4. Configure the already approved paid Gemini model and purpose flags with the
   existing paid-project key, sharedUSD5 reservation ledger and exact test-account
   allowlist. Keep unrelated push/closure maintenance disabled until its concrete
   executable authority and proof exist. Never expose credentials in artifacts.
5. Create the approved clearly labelled internal QA account on this DEV/ALPHA
   target, preserving production isolation. Grant only the approved owner support
   operator role through the exact service RPC and check the recorded receipt.
6. Execute the bounded probes in PAID_PROBE_PLAN.md: explicit text, visible/editable
   speech transcription before Send, live card, complete review, one publication,
   worker application/selection/Agreement, private text/photos and selected support
   evidence. Check actual provider usage after each controlled paid probe; retain
   unknown reservations and never auto-replay a paid request.
7. Build from the exact final saved source, attest Git/Metro/Hermes/assets/signer,
   install and verify real two-account native E2E against canonical DEV/ALPHA.
   Deliver that connected APK for the owner device test.

Implementation boundaries remain as decided: original Entry/mascot/animation/HOME;
email Auth; self-reported identity; existing urgent flag only;6 private photos per
message,10MiB input,1600px derivative without metadata; no permanent voice files,
new media types, invented legal identities or30-day history deletion. AF-D22
account closure still needs an applicable deletion/anonymization proof; the old
RETAINED_RESTRICTED relational result alone is not such a proof.

LIVE_MIGRATION_POSTFLIGHT_PLAN.md contains exact backfills, read-only checks,
policy/budget/session fences and stop conditions. Backend authorization is closed;
technical proof, actual deployment and the connected APK are still work in progress.
